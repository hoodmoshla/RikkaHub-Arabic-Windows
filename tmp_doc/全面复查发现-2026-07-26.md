# Rikkahub Desktop 全面复查发现(2026-07-26)

> 配套计划:`全面复查计划-2026-07-26.md`(工作协议、严重度定义、阶段清单见彼)。
> 基线 commit:e153cea。只查不改;修复在阶段九统一规划。

## 阶段一:服务端基座与生命周期

覆盖:bootstrap.ts、server.ts、api/(router|request|auth|static|sse|logs)、persistence/
(json-store|state-load|instance-lock)、foundation/(paths|platform|net|utils)、updates、
observability/app-errors、Tauri 壳启动等待段(lib.rs 相关行)。

### R1-1(P1)壳 20s 就绪超时 × 首启同步迁移链 = 重数据老用户可能永久无法启动
- 证据:`web-ui/src-tauri/src/lib.rs:38` `SIDECAR_READY_TIMEOUT = 20s`;超时后
  `show_startup_error` + `handle.exit(1)`(lib.rs ~591),而 sidecar 被 Job Object
  (KILL_ON_JOB_CLOSE,lib.rs:289-)连带击杀。服务端侧 `bootstrap()`(含 loadState 全部
  一次性迁移)在 `Bun.serve` 绑定端口/打印 `RIKKAHUB_PORT:` 之前同步执行(server.ts:26)。
- 场景推演:1.4.1→1.5.0 首启要跑 file-dedup(state-load.ts:354-,对 files/ 全部同尺寸组
  做同步 sha256——截图用户几 GB 很常见)+ 抽取旁车迁移 + 路径自愈;pre-1.2.6 直升用户还有
  SQLite 灌库(单事务,全或无)。任何一项超 20s → 壳报"未启动完成,请重试"并杀进程;
  单事务迁移下次从头再来 → **每次启动都超时被杀的死循环**,用户视角=升级后应用彻底打不开。
- 影响面:数据量大的忠实老用户,恰是最不能得罪的人群。
- 修复方向(候选):① 服务端改为"先绑端口打标记,再跑迁移,期间 API 返回 503/前端显示
  迁移进度页";② 壳把固定超时改为"子进程存活即继续等待+可视化等待提示";③ 大迁移分批
  提交(灌库按 N 条一事务,file-dedup 记录进度)。①+② 组合最彻底。

### R1-2(P1)state.json 恢复链新鲜度错序 + recovery/applied 文件永久残留(含全量 API Key)
- 证据:`pc-server/persistence/json-store.ts:92`(八连败后写 `state.json.recovery-<ts>.json`,
  全量 state 含所有供应商 apiKey);`json-store.ts:140-169` 恢复链**无条件**先用任意
  recovery-*.json,再 daily.bak,再 pre-sqlite.bak;`json-store.ts:176-195` 启动清扫只认
  `*.tmp`,recovery-*.json 与用过归档的 `.applied-<ts>` 无任何清理路径。
- 场景推演:某天磁盘满/杀毒锁文件导致一次八连败 → recovery 落盘;之后恢复正常,该文件
  永久残留。数月后 state.json 意外损坏 → 恢复链优先采用**数月前的 recovery**,而昨天的
  daily.bak 明明更新却排在其后 → 用户设置/密钥静默回滚数月,且没有任何提示告知回滚幅度。
- 附带:含明文 API Key 的文件(recovery/applied/daily.bak/pre-*.bak)在磁盘上无限期累积,
  用户拷贝 pc-data 求助/迁移时泄露面变大。
- 修复方向:① 成功落盘后删除所有 recovery-*(它们已过时);② 恢复链把 recovery/daily.bak
  全体候选按 mtime 统一排序取最新;③ .applied-* 与超龄 recovery 纳入启动清扫;
  ④ 恢复成功的 reportError 里写明所用备份的落盘时间,让用户知道回滚到了哪天。
- 补充(用户提问引出,2026-07-26):迁移安全快照缺乏统一保留策略。`pre-sqlite.bak`
  (含全部会话,重用户几十上百 MB)与 `pre-memory-split.bak` 永不过期,救援价值随时间
  贬值却持续占盘,用户翻看 pc-data 会觉得目录被污染。活数据本身无双份(迁移是搬家:
  字段删除/旁车/去重),问题纯在快照生命周期。方向:统一退役规则(迁移标记写入且活库
  健康运行 N 次启动后归档/删除)+ 备份类文件收进 backups/ 子目录用可读命名;另阶段五
  需核对 updates/ 缓存的历代安装包是否清理(潜在同类累积)。

### R1-3(P2)静态服务路径前缀校验无分隔符(经典穿越模式)
- 证据:`pc-server/api/static.ts:22-23` `resolve(staticRoot, requested)` 后仅
  `target.startsWith(staticRoot)`。`staticRoot=/x/build/client` 时 `/x/build/client2/...`
  也通过校验。当前打包布局无同前缀兄弟目录,实际不可利用,但这是审计器一眼红的模式,
  未来目录布局变化即变洞。
- 修复方向:`startsWith(staticRoot + sep)` 或 `!relative(staticRoot, target).startsWith("..")`。

### R1-4(P2)壳启动失败诊断误导 + 死契约标记
- 证据:`pc-server/server.ts:213` 输出 `port_in_use:<port>` 且注释声称"Tauri 壳解析此标记
  显示友好对话框"——`lib.rs` 全文无该字符串;壳对 child_dead **一律**弹"端口被占用"文案
  (lib.rs:173-198),但 sidecar 退出码 3=数据目录被另一实例锁定、1=其他启动失败;
  release 构建(`windows_subsystem="windows"`)下 stderr 不可见,真实原因(实例锁的中文
  提示)用户永远看不到。
- 场景推演:用户双开(或残留孤儿进程持锁)→ 弹"端口被占用,请换端口" → 用户改端口重启
  → 依旧失败 → 无解循环。
- 修复方向:壳区分退出码/解析标记行(如 `RIKKAHUB_FATAL:<code>:<message>`)分诊文案;
  顺手兑现或删除 `port_in_use:` 死契约。

### R1-5(P2,待阶段二定级)流式广播 pendingBroadcasts 条目在异常路径驻留
- 证据:`pc-server/api/sse.ts:20-57`:`flushNodeBroadcast` 只清 timer 不删 Map 条目;
  唯一删除点是 `broadcastNodeUpdate`(终态 flush)→ 若生成异常中断未走到终态 flush,
  条目携带整个 Conversation 对象引用永久驻留(整棵消息树 pin 在内存)。
- 待验证:orchestrator(483/499/627 三处调用)的终态 flush 是否在 finally 中覆盖所有
  退出路径(异常/abort/进程级兜底)。若覆盖则降 P3(仅防御性),阶段二核对。
- 【阶段二收口】已核对:正常/中止/失败路径 finally 均触发终态 flush;仅"流式中删会话/
  导入清库"路径泄漏。**降级 P3**,详见 R2-4。

### R1-6(P3)addLog 每条请求日志触发全量 state 序列化落盘
- `pc-server/api/logs.ts:116`:addLog 为累加 stats 计数调用非节流 `saveState()`——每条
  模型/搜索/MCP 请求日志都全量 JSON.stringify 整个 state 并写盘(coalesce 压到最多两次,
  但高频工具调用期间仍是放大的 IO)。可改 `scheduleThrottledSaveState()`。

### R1-7(P3)auto 代理模式下 fetch 拦截器内同步 spawn reg query
- `pc-server/foundation/net.ts:35-52,76-86`:readSystemProxy 用 Bun.spawnSync 读注册表,
  2s TTL 过期后的首个出站 fetch 阻塞事件循环数十 ms(两次 reg query);多路流式并发时
  可能表现为周期性微卡。可改异步后台刷新缓存。

### R1-8(P3)mime() 类型表缺失 woff2/ttf/json/wasm
- `pc-server/api/request.ts:32-41`:自定义字体/未来 wasm 资源以 octet-stream 兜底;
  字体浏览器容忍(仅告警),`.wasm` 一旦引入会因 instantiateStreaming 强制 MIME 校验而失败。

### R1-9(P3)注释锈蚀(基座层)
- `api/sse.ts:203-204` 引用巨石时代行号 "server.ts:10358/9601";`api/sse.ts:3` 顶注
  "generating 仍从 ../server 导入"——实际已改从 conversations/generation-state 导入。

### R1-10(P3)WS 消息处理器 JSON.parse 未防护
- `pc-server/server.ts:176`:ASR WebSocket 文本帧 `JSON.parse(data || "{}")` 无 try/catch,
  异常帧靠进程级 safety net 兜底(不崩,但以"未捕获异常"级别进错误中心,噪声+误导)。

### R1-11(P3)normalizeState 每次启动无条件正则改写用户提示词
- `pc-server/persistence/state-load.ts:138-139`:titlePrompt/suggestionPrompt 中的
  "not exceed 10 characters" 每次启动被替换为 15/18——用户如果有意写 10 会被反复静默改掉。
  应改为一次性迁移(标记门控)或仅对未自定义的默认值生效。

### R1-12(P3,阶段五证实)搜索服务 backfill 无删除豁免——删掉的内置项重启复活【已修复】
- `state-load.ts:186-204`:tinyfish/firecrawl/grok 按 type 不存在即补——若 UI 允许删除
  搜索服务,删掉的内置项每次重启复活。需核对搜索服务的删除路径后定级。
- 【阶段五核对 2026-07-27】服务端确有删除端点(settings.ts:566 settings/search/service/:id
  DELETE,不区分内置/自建),故复活路径成立:删 tinyfish → 重启 → 又回来了,用户意图
  被每次启动推翻。定级 P3(烦人但无数据损失)。修复方向:backfill 由"每次启动按 type
  补"改为一次性迁移(打标记后不再补),或维护 dismissedBuiltinSearchTypes 墓碑集。
  前端是否在 UI 上暴露删除按钮由阶段八顺带确认,不影响本定级。
- 【修复记录 2026-07-27,二次迭代为终极方案】第一版用 appliedMigrations 一次性迁移
  标记,但自查发现致命缝:pc-backup.json 不导出 appliedMigrations(backup/import.ts:114
  注释明言),备份恢复走 normalizeState 时标记丢失 → 补齐重跑 → 删掉的服务在恢复时
  复活——与 R4-1 同类的"标记不随备份走"问题。终极版改为【设置内墓碑】:
  ①settings 新增 dismissedSearchServiceTypes(小写 type 数组),随 settings 进备份
  天然往返,重启/恢复/跨机迁移都不推翻删除意图;②删除端点在该 type 无存活实例时记
  墓碑,手动重加该 type 时撤销;③state-load 的内置服务补齐与空列表回填均豁免墓碑,
  老数据无字段 → 空墓碑,行为与旧版完全一致;④撤销第一版的迁移标记代码(冗余清理),
  新增内置服务只需登记 preset,墓碑机制天然防复活。新增 search-tombstone.test.ts
  4 用例锁语义(无墓碑照补/有墓碑不复活/空列表回填豁免/规范化)。

### R1-13(P2)数据目录残留物全量清单与保留策略缺失(用户提出的专项,2026-07-26)
历次重构(1.2.6/1.3.0 会话入 SQLite、1.3.2/1.4.0 记忆拆分、1.5.0 附件去重/旁车)各自留下
迁移快照,均无退役机制。逐类盘点(代码证据齐全,按处置分三档):

**A. 活数据/功能文件(绝不能动)**
state.json、rikka_hub.db(+运行期 -wal/-shm)、files/、memory/ 三 JSON、skills/、fonts/、
device-id.txt、skip-version.txt、models-dev-cache.json、pc-server.lock(退出即删)、
rikka_hub_cached.db(**不是残留**——安卓 schema 模板,导出 zip 里的 rikka_hub.db 由它生成,
删了则 PC→APP 备份不含会话,backup/export.ts:206-211)。

**B. 有界备份(单代滚动/一次性,设计合理但永不退役)**
- state.json.daily.bak(每日滚动单代,json-store.ts:131)——合理,保留。
- state.json.pre-import.bak(每次导入覆盖,import.ts:62-66)——单代,末次导入后永驻。
- rikka_hub.db.pre-import.bak(每次导入 VACUUM INTO 覆盖,conversations/index.ts:513-525)
  ——单代但体积=整库,重用户数百 MB 永驻。
- state.json.pre-sqlite.bak(1.2.6 一次性化石,含全部会话,数十~数百 MB)——仍被
  probeConversationsDbOrRecover 当最后兜底引用,是"载重"的,不能简单删。
- state.json.pre-memory-split.bak(1.3.2 一次性化石)——迁移完成后无任何代码引用,纯遗物。

**C. 无界累积(真正的污染源)**
- state.json.recovery-*.json 及 *.applied-*(R1-2,含全量 API Key,无人清)。
- rikka_hub.db.corrupt-<ts>(损坏隔离取证,conversations/index.ts:39,单个可数百 MB,无人清)。
- memory/*.corrupt-<ts>(memory/index.ts:56,无人清)。
- updates/ 历代安装包:下载的 .exe/.tar.gz 每个版本一份(~80-100MB),extracted-* 只在
  重下同名包时清(update.ts:223);**没有"版本 ≤ 当前即删"的清扫**——升级越勤,堆积越多。
- state.json.*.tmp——已有 10 分钟年龄门清扫(1-4),此类无问题。

**修复方向(阶段九细化;安全第一原则)**
1. 任何清理只碰"冗余/可再生/已过期"三类,绝不触碰唯一副本;每次清理留 info 级错误中心痕迹。
2. recovery-*:成功落盘即删(它必然过时);applied-*/corrupt-* 保留最新一份+超龄(如 30 天)清;
   corrupt 大文件可提示用户而非静默删。
3. updates/:启动时删除版本号 ≤ 当前 APP_VERSION 的安装包与对应 extracted-*。
4. 化石快照(pre-sqlite/pre-memory-split):健康门槛退役——活库连续 N 次启动健康(探针通过)
   后,压缩归档到 backups/ 或提示可删;pre-sqlite 退役前需先给恢复链补充替代兜底(如定期
   VACUUM INTO 的活库快照,新鲜度远优于化石)。
5. 目录卫生:全部备份/快照类文件收敛到 pc-data/backups/ 子目录,命名带用途与日期,
   用户一眼可读;根目录只留活数据。
6. 待阶段四核对:files/ 孤儿附件(会话删除后不再被引用的物理文件)是否有 GC——这可能是
   比备份更大的隐性占用。

### 阶段一小结
- 总体评价:基座层达到高水准——显式 bootstrap 编排、实例锁先于迁移、原子写+多级恢复梯队、
  连接预算纪律、优雅停机双路径(信号+回环端点)幂等刷盘、Web 鉴权设计规范(HMAC 无状态
  token/timing-safe/限速)。骨架是"成熟商业应用"级别的。
- 需要处理:P1 两条(R1-1 升级死循环、R1-2 恢复链错序+密钥残留)都集中在"极端但真实"的
  存量用户场景,恰是本次重构最在意的人群;P2 三条为防御纵深/诊断质量;其余为卫生项。
- 条数:P1×2,P2×4(含 R1-13 数据目录卫生专项),P3×6,待验证×2(R1-5 并入 P2 计数)。

## 阶段二:会话域(服务端)

覆盖:conversations/(index|working-set|orchestrator|helpers|auxiliary 抽查|read-queries|
fts|stats|generation-state)、api/handlers/conversations.ts、SQLite schema、脏标记 flush、
安卓端 truncate 机制对照。

### R2-1(P1)truncateIndex 在消息树缩短后不钳制 → 上下文静默全空【已修复:随 R2-7 整体移除】
- 证据:编码侧 `inference-engine/conversation-encoding.ts:101-103`
  `applyTruncateIndex = nodes.slice(truncateIndex)`,无上界钳制;而全部缩树路径都不调整
  truncateIndex:regenerate(handlers/conversations.ts:267 及 truncateConversationForRegenerate,
  conversations/index.ts:600-616)、编辑消息(handlers:348 slice)、删除消息(handlers:309-315
  filter)。钳制只存在于 truncate 端点自身(nextTruncateIndex)。
- 场景推演:10 节点会话,用户在末尾"清除上下文"(truncateIndex=10);之后编辑第 3 节点的
  消息(树截到 4 节点)→ 自动触发重新生成 → slice(10)=[] → **模型只收到 system prompt,
  连刚编辑的用户消息都不在上下文里**,回复必然文不对题且无任何报错;前端分割线因索引
  越界不渲染,用户无从发现和撤销。fork 继承越界 truncateIndex(handlers:452 spread),同病。
- 影响面:用过"清除上下文"再编辑/回退/删除消息的任何用户;静默错误,难以自查。
- 修复方向:① 所有缩树路径统一钳制 `truncateIndex = Math.min(truncateIndex, messages.length)`
  (或 ≤0 归 -1);② 编码侧与前端分割线各加防御性钳制;③ 与 R2-7 一并做产品决策。
- **处置(2026-07-27)**:用户决策跟随安卓删除整个机制(见 R2-7),truncateIndex 全链路
  移除后本缺陷不复存在,无需钳制。

### R2-2(P2)fire-and-forget 续体在 checkout 释放后写会话(工作集双实例风险窗口)
- 证据:`api/handlers/conversations.ts` 子路由块 try/finally 在返回时 release(498-500),
  但 messages POST(146-153)与 translate(381-424)的 `void (async ...)` 续体在 release
  **之后**继续 await(OCR/翻译,可长于 60s)再写 conversation 并调 generateAnswer。
- 场景推演:发消息后立即切走会话(SSE 关) → 实例 refs=0、非生成中、无脏标记 → 60s 后被
  sweep 清出;慢 OCR 完成后续体写的是**孤儿对象**,generateAnswer 的 checkout 从活库装出
  第二实例进注册表 → 流式内容写进孤儿,flushConvDirty/completeConversationGeneration 却
  persist 注册表里的陈旧实例 → **屏上看得到回答,重启后消失**(UI 与磁盘分叉)。
- 这正是 working-set 声称的核心不变式("持有实例期间 checkout 必返回同一实例")被
  fire-and-forget 模式绕过——防护应是结构性的,不该依赖"OCR 一般不超过 60s"。
- 修复方向:续体自身 checkout/release 包住全生命周期;或 generateAnswer 入口校验
  checkout 返回实例 === 传入实例,不一致时以注册表实例为准重放。

### R2-3(P2)OCR 窗口内连发两条消息 → 并发生成写同一节点
- 证据:messages POST 的 generateAnswer 在 OCR 续体里异步触发(handlers:146-153)。
  handler 同步段的 abort 守卫(132-133)只能中止**已登记**的生成;A、B 两条消息在 OCR
  期间相继进入时,B 入场时 A 的 generateAnswer 尚未 set generating,无可中止。
- 场景推演(需 OCR 开启+偏慢):两续体先后调 generateAnswer 并发跑;A 见末节点为 userB
  → 新建 assistant 节点;B 见末节点为 A 的空 ASSISTANT 占位 → `ensureAssistantGenerationNode`
  复用之 → **两路流式交错写同一 message,parts 交叉污染**;generating Map 后写覆盖前写,
  A 的 controller 失去登记、无人能停。
- 修复方向:generateAnswer 入口对 generating.has(id) 自带"先 abort 旧流"守卫(把 2-1
  的端点级纪律下沉为编排器不变式);或按会话串行化生成队列。

### R2-4(P3)R1-5 定级收口:pendingBroadcasts 仅在"流式中删会话/导入清库"路径泄漏
- orchestrator finally(623-628)在会话已删时提前 return,跳过终态 broadcastNodeUpdate
  (唯一的条目删除点)→ 该条目连同已删会话整树永久驻留。正常完成/中止/失败路径都会
  清理。频率低、有界(每次删流式中会话泄 1 条),降级 P3。
- 修复方向:deleteConversationsById/clearWorkingSet 时按 convId 前缀清 pendingBroadcasts;
  或 flushNodeBroadcast 在非生成期直接删条目。

### R2-5(P3)会话操作端点残留大量无效 saveState()
- pin/title/truncate/move/system-prompt/stop/regenerate/fork/tool-approval/删除等每个端点
  都调 `saveState()`(全量序列化 state.json),但 state.json 已不含会话数据——除
  bumpAnalyticsMsgCount(计数)外均为纯浪费 IO(与 R1-6 同根:遗留调用未随 DB-first 清理)。
- 修复方向:逐点删除或收敛到真正改 state 的位置;与 R1-6 一并处理。

### R2-6(P3)删除会话不关闭其已打开的详情 SSE 连接
- `helpers.ts:35` 仅 `conversationClients.delete(id)` 摘掉集合,不对 controller 调 close;
  已打开的详情流从此只收心跳,靠前端收到 invalidate 后自行切走。连接悬挂有限但不体面。
- 修复方向:摘集合前逐 controller close(对齐背压断开的既有模式)。

### R2-7(P3,产品/互操作)truncateIndex 机制与最新安卓已分叉【已修复:跟随安卓删除】
- 决定性证据:安卓提交 `1f572a80`(2026-03-03)"refactor(chat): 移除截断上下文功能——
  删除 truncateIndex 相关的全部逻辑与 UI,包括截断分隔线、'清空上下文'菜单项、数据库
  字段及迁移,并将数据库升级至版本 17"。这是作者有意识的产品决策,非重构遗漏;当前
  安卓字符串资源已无任何"清除/清空上下文"入口。
- 安卓的职能替代(非同等机制):①**压缩上下文** `ChatService.compressConversation`
  ——手动触发,压缩模型把旧消息总结成摘要、保留最近 N 条(可输入,默认 32),**破坏性**
  (UI 警告"压缩上下文将重置当前对话中的所有消息"),且作者持续迭代中(分 chunk、保留
  最新消息配置);②**助手级 contextMessageSize** 滚动窗口——`GenerationHandler.kt:388`
  `limitContext` 只发最近 N 条,并新增截断/提示词缓存警告文案。
  即安卓用"破坏性压缩 + 滚动窗口"替代了"非破坏性插标记";轻量清除方式无继任者。
- 互操作现状:PC→APP 时 `truncate_index` 被安卓直接丢弃(列不存在),不损数据;
  APP→PC 无此字段输入。往返(PC→APP→PC)后用户设置的清除点**静默永久消失**——
  功能性丢失,非数据损坏。
- 修复方向:产品决策——保留为 PC 特性 or 跟随安卓删除。
- **处置(2026-07-27,用户决策:跟随安卓)**:两个替代机制 PC 早已具备且与安卓同构
  (压缩对话历史 `conversations/auxiliary.ts compressConversation`,算法逐行同源:
  keepRecent 默认 32、256 条分 chunk、摘要作 USER 消息+保留尾部、破坏性替换;
  助手级 contextMessageSize 于 `conversation-encoding.ts` 切片)。执行内容:
  ① 服务端删 Conversation.truncateIndex 字段、truncate 端点、applyTruncateIndex/
  nextTruncateIndex 及 compress 的 truncateIndex=0 写入;② 活库一次性迁移
  `dropTruncateIndexColumnIfPresent`(PRAGMA 探测 + ALTER TABLE DROP COLUMN,幂等,
  失败仅告警不阻断,对齐安卓 Migration_16_17);③ 备份对齐:pcdump 导出 schema 去列,
  导入侧显式列名 SELECT 天然兼容带列的老 dump(已演练验证);安卓导入侧删字段填充;
  ④ 前端删分割线组件、"清除上下文"菜单项、相关 i18n 词条与类型字段;⑤ 测试:删
  truncate-index/truncate-toggle 两个死测试,修 6 个测试与 verify-persistence-fixes
  场景脚本(老 state.json 种子保留 truncateIndex 字段模拟老格式输入,断言改为字段
  已被移除)。老库迁移演练(带列库→打开→列删数据无损→幂等)与老 dump 导入演练全 PASS,
  183 测试 0 失败。

### R2-8(P3)分页列表每请求全量元数据扫描+JS 过滤排序
- `handlers/conversations.ts:57-68`:每次 paged 请求把该助手全部会话元数据读出、JS 过滤/
  排序/切片(注释说明为保 Unicode 语义等价的有意取舍)。5k 会话毫秒级可接受;规模 ×100
  (未来 Agent 化多会话)时需 SQL 化(LIKE + collation 或 FTS 标题索引)。备忘,不动。

### 阶段二小结
- 总体评价:会话域是重构质量最高的部分之一——REPLACE 级联陷阱的修复与回归测试、
  working-set 四条件清扫、端点级 abort 守卫、FTS 自愈、stats 分片让出、脏标记与删除的
  竞态处理都有清晰纪律注释。骨架可靠。
- 需要处理:P1×1(R2-1 truncateIndex 越界,触发链常见:清上下文→编辑/回退);P2×2
  (R2-2/R2-3 都源自"fire-and-forget 续体在 release 后运行"这一个结构模式,可一并修);
  P3×5(含 R1-5 收口降级)。
- 条数:P1×1,P2×2,P3×5。其中 R2-1+R2-7 已于 2026-07-27 随"跟随安卓删除 truncateIndex"
  一并解决。

## 阶段三:推理引擎与工具链

> 覆盖:inference-engine/(providers 1653 行、message-builder 1019、tool-loop 274、
> conversation-encoding 231、parts 152、events 69)、tools/ 全部 12 文件、search/index(1270)、
> media/(image-gen|tts|asr)、model-providers/(index|checks)。均逐行阅读;search 的 17 个服务
> 实现按模板抽查(failover 骨架+tavily/rikkahub/tinyfish/bing_local 全读)。

### R3-1(P2)Claude/Google 主链路无任何超时看门狗(OpenAI 独有)

- 证据:providers.ts:1554-1575(OpenAI fetchRound:头超时 180s/600s + 外部 signal 桥接)、
  providers.ts:1117-1133(readOpenAiStream 独有 120s 空闲超时);对照 providers.ts:442-447
  (Claude fetchRound:裸 fetch 仅传 signal)、providers.ts:774-779(Google 同)。
  readClaudeStreamingRound / readGoogleStreamingRound 的 reader.read() 均无空闲超时;
  generation-state.ts 亦无 watchdog。辅助流(标题/建议/压缩)三家都有 600s 硬上限
  (AUX_STREAM_TIMEOUT_MS),唯独主对话链路的 Claude/Google 是裸奔。
- 场景推演:上游黑洞(TCP 握手成功但永不回头部,或流中途静默不断连——劣质中转站常见)。
  OpenAI 路径 180s/120s 后自动报错落库;Claude/Gemini 路径则永久悬挂:generating=true
  挡住 working-set 清扫与该会话的再次生成,消息卡在"正在生成回复"。用户在场可点停止;
  切走会话/关窗后就是不可自愈的僵尸生成(占引用、占上游连接)。
- 影响面:所有 claude/google 类型 provider 的对话生成;商业应用不可接受的"无人值守挂死"。
- 修复方向:把 OpenAI 的两层超时下沉为 tool-loop 骨架能力(fetchRound 由骨架统一包
  头超时+signal 桥接;readRound 统一 120s 空闲包装),adapter 只留差异参数。tool-loop.ts
  的"行为冻结纪律"注释本就预告了这类统一属单独修复提交——现在是兑现时机。可顺带评估
  把 nonStreamFallback(流式失败降级非流式)推广到 Claude/Google(低优先,协议差异大)。

### R3-2(P2)MCP SSE 握手实际预算是 1 秒,不是注释声称的 15 秒

- 证据:mcp.ts:72-110 readMcpSseUntilEndpoint。外层 timeoutMs=15000 只在循环头检查;
  但每次 reader.read() 被 Promise.race 包了一个 1000ms 的 reject(mcp.ts:81-86),
  该 reject 无 try/catch 捕获,直接把整个函数抛出 "SSE MCP endpoint event timeout"。
  即:服务器静默超过 1 秒 → 握手失败,15s 预算永远达不到;且 setTimeout 从不 clearTimeout,
  泄漏定时器与挂起的 read promise。
- 场景推演:SSE 型 MCP 服务器冷启动(serverless/容器拉起)或跨洋高延迟,endpoint 事件
  1 秒内到不了 → 用户配置正确却永远"连接失败",且报错文案与真实原因(太慢)不符。
- 影响面:所有 type="sse" 的 MCP 服务器;streamable_http 不受影响。
- 修复方向:把 1s race 的 reject 改为循环内 catch-continue(它本意应是"每 1s 醒来检查
  外层预算"的 tick),以 15s 总预算为准;clearTimeout 收尾。

### R3-3(P2)MCP SSE 的 ssePostEndpoint 缓存永不失效,且被持久化进 settings

- 证据:mcp.ts:112-144(首次握手后 server.ssePostEndpoint = endpoint,直接写在
  state.settings.mcpServers 的元素上;后续 mcpSsePostEndpoint 永远命中缓存);
  mcp.ts:276-282(tools/call 失败重试只清 mcpSessionCache,不清 ssePostEndpoint);
  api/handlers/settings.ts:322(该字段被显式序列化保存,前端保存时原样回传)。
- 场景推演:SSE 型 MCP 服务器的 POST endpoint 常带会话标识(如 /messages?sessionId=x)。
  服务器重启后旧 endpoint 失效 → 此后每次 tools/call、tools/list 全部失败;重试逻辑
  换 session 不换 endpoint,救不回来;重启 PC 应用也没用(endpoint 已持久化)。唯一出路
  是用户手动改一遍服务器配置触发重新握手——用户不可能知道这一点。
- 影响面:所有 type="sse" 的 MCP 服务器的长期可用性。
- 修复方向:①失败重试路径同时清 ssePostEndpoint(内存+settings)再重新握手;
  ②该字段本质是运行时缓存,不应持久化——迁到 mcpSessionCache 同级的内存 Map,
  settings 里的旧值一次性清理(顺应"用户数据文件夹干净"原则)。

### R3-4(P2)用户点"停止"后,Claude/Gemini 本轮工具仍会执行(副作用无法拦截)

- 证据:tool-loop.ts:81-83 adapter.abortCheckAfterRead 注释自认差异冻结
  ("Claude/Google: false——原实现只在轮首检查,abort 后本轮工具仍会执行");
  providers.ts:501/809(两家 false)、providers.ts:1644(OpenAI true)。
  读流中 abort 会因 signal 传给 fetch 而中断;但"流读完→工具执行前"这个窗口,
  Claude/Google 不检查 signal,tool-loop.ts:216-259 的执行循环内部也不检查。
- 场景推演:模型宣布调用一个有副作用的工具(MCP 写操作/剪贴板覆盖/TTS 朗读),
  用户看到工具卡出现立刻点停止——若该工具无需审批,Claude/Gemini 路径照样执行完
  才抛 abort;多工具批次则整批执行。用户的"停止"心智模型被违背。
- 影响面:claude/google 主链路 + 三家非流式路径(fetchClaudeTextWithTools/fetchOpenAiText
  的工具循环同样只在轮首查 abort)。
- 修复方向:runStreamingToolLoop 的工具执行 for 循环每次迭代前检查 signal?.aborted
  并抛 AbortError(半消息落库路径已有,orchestrator 会正常收尾);三家统一后删除
  abortCheckAfterRead 冻结点位。属 tool-loop 注释预告的"行为修复单独提交",与 R3-1 同批。

### R3-5(P3)readOpenAiStream 空闲超时报错文案与注释陈旧

- 证据:providers.ts:1121(timeoutMs=120_000)vs :1127 报错文本 "no data received for
  10min";:1118 注释引用 "server.ts:6063"——该文件早已拆分,行号失效。
- 影响面:用户看到的错误信息自相矛盾(2 分钟就报"10 分钟无数据");误导排障。
- 修复方向:文案改 "2min",注释改指向本文件普适理由。一行级,随 R3-1 统一超时时自然消亡。

### R3-6(P3)死代码与微冗余三处

- ①tool-loop.ts:272 fallbackToolCallId 导出后全仓零引用(adapter 直接用 id());
  ②providers.ts:368/388/407/421 readClaudeStreamingRound 的 currentEvent 只写不读,
  靠 void currentEvent 压 lint——纯残留;
  ③providers.ts:1236 fetchOpenAiAuxiliaryStream 内 appendUsageFromRaw(undefined, raw)
  首参恒 undefined,函数首行即 return,恒为 no-op。
- 修复方向:直接删除。清理级,不改行为。

### R3-7(P3)parseSseChunks 假设"一行一个 JSON",跨行 data 会被静默丢弃

- 证据:providers.ts:971-989——把 frame 内多条 data: 行 join("
") 后又按行 split,
  逐行 JSON.parse;一个跨多行的 data JSON 会每行解析失败被容错 catch 吞掉。
  对照 readClaudeStreamingRound(providers.ts:379-390)对多行 data 的处理是正确的
  (join 后整体 parse)。受影响读器:readGoogleStreamingRound / readOpenAiStream /
  readClaudeStream(辅助)/ readOpenAiSseTextIntoMessage。
- 场景推演:当前三家官方上游都发单行 data,不触发;某些中转站/代理若美化 JSON 输出
  (SSE 规范允许跨行 data)则内容整段丢失且无任何报错。
- 影响面:低概率、高隐蔽。修复方向:与 Claude 主读器对齐——data 行 join 后整体 parse,
  失败再走容错;不改变单行行为。

### R3-8(P3)多轮工具会话的 usage 口径:每轮覆盖,completionTokens 只剩最后一轮

- 证据:tool-loop.ts:184-187(每轮 result.usage 经 sink 覆盖 message.usage);
  OpenAI 路径 appendUsageFromRaw(providers.ts:151-168)同为覆盖语义。
- 推演:一次 5 轮工具往返,前 4 轮的 completion tokens 不计入最终 usage;
  promptTokens=最后一轮恰好符合"当前上下文"显示语义(分子),但 completion/total
  作为"本条消息花费"被低估。安卓 Usage.merge 同为"新值>0 才采用"的覆盖式,
  两端口径一致——记录在案,若未来做用量统计再统一改为跨轮累加。

### R3-9(P3)部署面注记:scrape_web 的 SSRF 面 + custom_js 无沙箱

- ①search/index.ts:951-1036 runScrapeWeb 对模型给出的任意 http(s) URL 直接服务端 GET
  (含 localhost/内网地址)。桌面单用户风险可忽略;Docker/LAN 部署下模型(或被注入的
  网页内容)可用它探测内网服务。②runCustomJsFunction(search/index.ts:92-112)用
  AsyncFunction 在服务进程内执行用户自配脚本,无沙箱(安卓端等价物走 QuickJS 沙箱);
  同属"用户自己配置自己负责",但 fetch 之外它还能触到 process/Bun 全局。
- 处置:不算当下缺陷,移交阶段五(部署面)统一评估:Docker 文档标注 + 可选的
  scrape 内网地址黑名单(默认关,对齐"跟安卓一样"的既有决策)。

### R3-10(P3)MCP 工具调用 30s 硬超时,长任务工具必死

- 证据:mcp.ts:164(postMcpJsonRpc 固定 30_000 abort),tools/call 无差别适用。
- 推演:生成型/爬取型 MCP 工具(执行 >30s)永远超时,模型收到超时错误后可能反复重试,
  烧轮次。对照:本地生图给了 300s、ASR/TTS 120s、辅助流 600s——MCP 是唯一还卡在
  30s 默认值的慢任务通道。修复方向:tools/call 单独放宽(如 300s)或按服务器可配。

### 阶段三小结

- 条数:P2×4,P3×6(无 P0/P1)。
- 最重要:R3-1(Claude/Google 主链路挂死无自愈——直接对标"成熟商业应用"的可用性底线)、
  R3-3(SSE MCP 一旦服务器重启即永久失效,且脏数据被持久化)、R3-4(停止按钮拦不住
  副作用工具,用户信任问题)。R3-1+R3-4+R3-5 同属 tool-loop 骨架统一批,可一次提交解决。
- 总体评价:推理引擎经 P1-5 骨架统一后结构清晰(adapter 模式、事件 sink 解耦、审批
  pre-scan、非流式降级),消息编码层对安卓的镜像标注详尽;工具链的审批/恢复/打断语义
  完整。遗留问题集中在"三家行为差异冻结未兑现统一"与 MCP SSE 类型的生命周期管理。

## 阶段四:文件域与备份互操作

> 覆盖:files/index.ts(885 行,全读)、backup/export.ts(624)、import.ts(967)、
> storage.ts(450)、file-refs.ts(50)、api/handlers/files.ts、state-load.ts 的文件域迁移段
> (repairRelocatedFilePaths / migrateExtractedTextToSidecars / migrateConversationsIfNeeded)。
> OCR 确认属辅助生成链路(auxiliary.ts,阶段二/三已覆盖),文件域无独立 OCR 引擎。

### R4-1(P1)恢复备份后 conversations-sqlite-1.2.6 迁移标记丢失,老用户下次启动"化石复活"

- 证据链(三段拼合):
  ①backupPayloadMetadataOnly(export.ts:74-96)不导出 appliedMigrations;
  ②两条恢复路径(applyBackupPayload import.ts:116-119、applyPcBackupFromExtractDir
  import.ts:199-202)都 setState(normalizeState(incoming)) 后只强制补回
  MEMORY_FILE_SPLIT_MIGRATION——恰恰漏了对称的 CONVERSATIONS_SQLITE_MIGRATION
  (注释自己写着"pc-backup.json 不导出 appliedMigrations,normalizeState 后丢失",
  修了记忆那半,没修会话这半);
  ③下次启动 migrateConversationsIfNeeded(state-load.ts:523-541):标记缺失 →
  state.json 又没有 conversations 数组 → 触发方案 B 兜底 recoverConversationsFromBak(),
  从 state.json.pre-sqlite.bak(1.2.6 迁移时代化石,设计上永久保留,json-store.ts:102
  自述"化石")读出全部老会话,经 migrateConversationsIntoDb 灌进活库。
- 场景推演(受害者=最核心的老用户群):1.3.0 前入坑的用户磁盘上必有 pre-sqlite.bak。
  他今天在 1.5.0 上恢复任意一份自己的 PC 备份(zip 或老 JSON)→ 恢复本身成功 →
  重启应用 → 几个月前删掉的会话全部复活;更糟的是同 id 会话被化石版本整体覆盖
  (migrateConversationsIntoDb 是"删旧节点+重插"语义,conversations/index.ts:451-478),
  备份里刚恢复的新分支被一年前的旧树顶掉。用户视角:"恢复备份把我的会话搞乱了",
  且无从理解因果(相隔一次重启)。
- 影响面:所有带 pre-sqlite.bak 的老用户 × 所有 PC 备份恢复路径(本地/WebDAV/S3 同源);
  无 bak 的新用户只是白跑一次空迁移+补写标记,无害。安卓 zip 合并路径不重建 state,
  标记不丢,不受影响。
- 修复方向:两条恢复路径在补 MEMORY_FILE_SPLIT 的同一位置补 CONVERSATIONS_SQLITE_MIGRATION
  (恢复流程本身就以 resetConversationsDbTo 让活库成为权威,标记语义完全成立)。
  一行级修复。可另议:方案 B 兜底把化石灌库前至少 reportError 告知用户(现状只有 console)。

### R4-2(P2)导入中途异常:暂存 state.conversations 残留,被后续 saveState 持久化

- 证据:applyPcBackupFromExtractDir 在 setState(建立暂存 state.conversations,
  import.ts:220 dump 读入)之后、finalizeConversationImport(import.ts:323)之前,
  还有附件 re-link 大循环(copyFileSync,磁盘满 ENOSPC 时机恰好最高)。此段抛异常
  直接 rethrow(import.ts:327-329),finalize 不执行:暂存不清、活库未重灌、
  pre-import 活库快照也没做。此后任意一次 saveState(R4-1 已证此时迁移标记缺失,
  json-store.ts:64 的排除门不生效)把整个备份的 conversations 数组写进 state.json;
  下次启动迁移门把它当 1.2.6 老数据合并灌进活库——半应用的备份 + 替换语义退化为合并语义。
- 场景推演:磁盘满导致恢复失败,用户看到报错后"先凑合用着",两天后重启——备份里的
  会话突然和现有会话混在一起,且设置已在失败那次被换成备份版(setState 已生效)。
- 修复方向:恢复路径包 try/catch,失败时 delete state.conversations(暂存作废)并从
  state.json.pre-import.bak 回滚 setState(快照 5-6 本就为此而设,现在只差"用上它");
  至少也要清暂存字段。与 R4-1 同批修。

### R4-3(P2)文档抽取是同步 CPU 重活,大 PDF 会冻结整个后端事件环【已修复:安卓 document 模块对齐】

- 证据:extractPdfText(files/index.ts:536-561)逐页 loadPage/toStructuredText 全同步,
  中间无一次 yield;上限 100MB(MAX_PDF_EXTRACT_BYTES)。调用点两处:上传请求内
  (api/handlers/files.ts:25,await 不改变单线程本质)与编码未命中旁车时的
  ensureExtractedTextAsync"后台"补抽(files/index.ts:461-470——所谓后台只是不阻塞
  本次请求,解析仍占死事件环)。DOCX/PPTX/EPUB 的正则/XmlPull 解析同理(量级略小)。
- 场景推演:用户 A 上传 80MB 扫描书 PDF,MuPDF 逐页解析 10-30 秒——期间所有会话的
  流式输出、SSE 心跳、其他 API 全部冻结;若同时有生成在跑,前端表现为"卡死幻觉"。
  3-4 把解析挪出编码热路径只解决了"何时卡",没解决"卡"本身。
- 影响面:多会话并行使用 + 大文档场景;单用户小文档无感。
- 修复方向(按性价比):①页循环每 N 页 await Bun.sleep(0) 让事件环喘息(几行,
  立刻把"冻结 30s"变"偶尔多几毫秒延迟");②中期把抽取挪进 Bun Worker(路线图的
  "流式上传+逐页解析"属同方向,本轮不做)。
- 【修复记录 2026-07-27】按用户指示做了完整的安卓 document 模块对齐,超出原修复方向:
  ①新增 files/zip-file.ts:中央目录随机访问 zip 读取器(对齐 java.util.zip.ZipFile 语义,
  含 zip64),打开只读 EOCD+中央目录,条目按需解压;②DOCX/PPTX/EPUB 三个解析器改走
  读取器,只解压需要的 XML 条目(嵌入图片/字体/媒体永不解压)——单文件内存峰值从
  "全部条目解压总体积"(最坏 GB 级)降到"最大单条目"(KB~MB 级),与安卓一致地
  【取消这三种格式的 100MB 上限】;顺带删除 extractSingleZipMemberStreaming(外部
  tar.exe/unzip 依赖)与 DOCX_STREAMING_THRESHOLD_BYTES(冗余清理);③PDF 逐页、PPTX 逐
  slide、EPUB 逐章节 await setImmediate 让出事件环——安卓靠后台线程,PC 单线程用协作式
  让出对齐,大文档解析期间流式/SSE 不再冻结。仍保留的两个上限均为平台差异:PDF 100MB
  (mupdf-wasm 只能从内存 buffer 打开,安卓原生按路径打开)、纯文本 100MB(两端都是
  整串进内存,JS 堆保险)。新增 files/zip-file.test.ts 7 用例(store/deflate/损坏条目/
  非 zip/三格式只解需要条目);190 服务端测试全过,tsc 干净。
  遗留(路线图,本轮不做):上传入口 request.formData() 仍整文件进内存——"流式上传"项。
- 【对齐程度残差(如实记录)】zip 域(DOCX/PPTX/EPUB)已完全对齐安卓语义,其余三处
  仍有明确差异,均属平台约束或路线图项,不是偷懒:
  ①并发模型:安卓解析跑真后台线程,PC 是事件环内协作式让出——页/slide/章节【之间】
  会让出,但单个条目的解析仍独占事件环(常规文档毫秒级无感;病态的单章超大 XML 仍可能
  卡数百 ms)。彻底对齐 = Bun Worker,但 bun build --compile 单 exe 嵌 Worker+wasm 组合
  风险未验证,归入路线图"流式上传+逐页解析"同批。
  ②PDF 打开方式:安卓 MuPDF 原生按路径打开(不进 JVM 堆),PC mupdf-wasm 只能整文件
  进 wasm 堆,故保留 100MB 上限。wasm 上游不支持流式打开前无解。
  ③XML 流式度:安卓把 zip InputStream 直灌 XmlPullParser(全程不持有完整 XML 字符串),
  PC 是"单条目解压成串再 tokenize"——内存 = 单条目级(几十 MB 档),非无界,可接受。

### R4-4(P3)空抽取结果不写旁车,扫描版 PDF 每次发送都全量重抽【已修复:进程内负缓存】

- 证据:writeExtractedTextSidecar 首行 if (!text) return(files/index.ts:445);
  纯图 PDF/解析失败恒返回 ""。编码路径每次未命中旁车都会再触发 ensureExtractedTextAsync
  (in-flight 去重只挡并发,不挡重复),即每次发送消息都把这本 90MB 扫描书完整
  重新 MuPDF 解析一遍——结果永远是空,永远不缓存。R4-3 的冻结按发送次数放大。
- 修复方向:空结果也落一个负缓存标记(如写入单字节哨兵或 .extracted.empty 旁车),
  读取侧命中即走 fallbackDocumentText;文件重传/手动重抽时自然失效。与 R4-3 同批。
- 【修复记录 2026-07-27】采用比原方向更保守的进程内负缓存(emptyExtractionAttempts
  Set):空结果本次运行只试一次,不落盘——避免把瞬时故障(wasm 初始化失败等)永久
  钉死,重启/重新上传自然重试。配合 R4-3 的逐页让出,即便负缓存未命中时的重抽也不再
  冻结事件环。

### R4-5(P3)导入备份时若有工具正在执行,过时节点可能回写进刚恢复的活库

- 证据:finalizeConversationImport(import.ts:900-915)abort+清脏+重灌+清 working set
  全程同步,同 tick 内无缝——纯流式场景安全。但 Claude/Google 的工具执行不检查 abort
  (R3-4 冻结点),工具跑完后 sink 的 tool_result/text 事件仍会 touchStream→markDirty,
  200ms 节流 flush 把旧会话节点 upsert 进刚 reset 完的新库(clearConvDirtyState 在
  reset 之前,挡不住 reset 之后新增的脏)。结果:恢复出的库里多出一条/一块备份里
  不存在的旧消息树残片。
- 影响面:极窄(导入瞬间恰有未审批慢工具在跑),但违背"恢复=回到备份时刻"语义。
  与 R2-2(释放后延续)、R3-4(abort 不拦工具)同根——R3-4 修掉后本条基本自愈,
  记录在案作为 R3-4 的又一条受益证据。

### R4-6(P3)附件 staging 名沿用原始 fileName,跨平台非法字符会让该附件进不了备份

- 证据:buildUploadStagingPlan(export.ts:496-505)zip 内文件名直接用 file.fileName
  (仅处理重名,不清洗字符);Windows 上 copyFileSync 遇 `:*?"<>|` 抛错,
  stageUploadFilesInto 计数上报(5-3 已做),但该附件永远缺席备份,用户无解。
  fileName 可来自 Linux/安卓上传(那边这些字符合法)。
- 修复方向:staging 名做一次跨平台字符清洗(backupName 机制本就与原名解耦,
  恢复端按 backupName 回链,清洗零副作用)。一行级。

### 阶段四小结

- 条数:P1×1,P2×2,P3×3。
- 最重要:R4-1(老用户恢复备份→重启后化石会话复活并覆盖新数据,直接踩"用户数据
  千万不能出问题"的红线;修复一行级,性价比全场最高)。R4-2 与 R4-1 同场景共振
  (都在恢复路径,同批修)。R4-3/R4-4 是"成熟商业应用"体感项:大文档用户的全局冻结。
- 专项核对结论(计划里的 corner case 清单):
  ①zip-slip:Windows 走 .NET ExtractToDirectory(内置越界校验)、Linux 走 unzip
  (自身拒绝 ../ 成员);backupName 消费侧另有显式 ../ 与分隔符拒绝(import.ts:278)——通过。
  ②>2GB 附件:5-7 批已全面流式化(hashFileSha256 分块、copyFileSync 内核拷贝、
  外部 zip/unzip),未见回归——通过。
  ③id 冲突:nextFileIdSafeFloor 三源取最大(内存账本/nextFileId/磁盘文件名),
  恢复永不覆写现有字节——通过(5-1 修复保持完好)。
  ④内容去重:导出按 sha256 共享 zip 条目、安卓导入按内容比对复用旧 id,
  PC↔APP 往返附件不再翻倍——通过。
  ⑤附件账本自愈:repairRelocatedFilePaths 的 byBasename 兜底覆盖 restored-/mcp-image-/
  generated- 等非 <id>.<ext> 命名——通过。
  ⑥旁车迁移(1.4.1→1.5.0):migrateExtractedTextToSidecars 幂等(迁完即删字段),
  老 JSON 备份的 extractedText 导入时也落旁车——通过。
  ⑦跨版本导入(含 truncate_index 时代的 pcdump):导入侧显式列 SELECT,多余列自动
  忽略——通过(阶段二已演练)。
- 阶段一遗留问题闭环(发现文档第 141 行"孤儿附件是否有 GC"):结论=无 GC,确认为
  真实磁盘泄漏点。直接删文件的端点已做字节+旁车清理(files.ts:78-91,5-7 修复),但
  删会话不删其引用的附件——账本条目与字节永久残留,仅在导出时被 orphanSkipped 跳过
  (不污染备份,但磁盘持续膨胀)。安卓端同样不回收(删会话仅删 Room 行),属"跟安卓
  一样"的既有语义;若做,方向是启动时低频扫描(复用 collectReferencedFileIds 的并集逻辑,
  引用为空且账龄超阈值才清)。并入 R1-13(数据目录卫生)一起决策,不单列新条目。
- 记录在案(不列为缺陷):导出暂存需要约 2 倍附件体积的临时磁盘(zip 流式方案的
  固有代价,失败有报错);generateRikkaHubDb 的 :memory:+serialize 峰值内存=安卓库
  体积(低频导出可接受);Linux 依赖外部 zip/unzip 二进制→移交阶段五 Docker 清单核对。
- 总体评价:备份域是全仓修复痕迹最密集的模块(5-x/批5/备份2.0 三轮加固),导出侧
  引用扫描+去重+自适应超时、导入侧快照+id 安全下界+URL 改写+降级语义都已相当成熟;
  剩余问题集中在"恢复与一次性迁移体系的耦合缝"(R4-1/R4-2)——恰是三轮加固都没
  照到的交界处。

## 阶段五:服务端横切与部署面

审查时间:2026-07-27。范围:server.ts 入口、api/(auth|router|request|static|sse|logs)、
handlers/(settings|memory|system|errors|update)、memory store、observability/app-errors、
app-config/*、assistants/index、updates、scripts/(bump-version|check-version-sync)、Dockerfile。
方法:通读 + 鉴权覆盖面矩阵推演 + 反代/容器部署场景推演。

### R5-1(P2)/api/app/shutdown 在反向代理后可被远程未鉴权触发

- 证据:server.ts:137-149,shutdown 端点在 Web 鉴权检查【之前】处理,唯一守卫是
  server.requestIP 为回环地址(设计意图:Tauri 壳不持有 token,只信本机)。但用户在
  同一台机器上架 nginx/caddy 反代时,所有远程请求到达 Bun 的 remote address 都是
  127.0.0.1——任何互联网客户端 POST /api/app/shutdown 即可让服务刷盘后自杀,不需要
  密码、不需要 Origin(curl 不带)。README 明确支持 --host/RIKKAHUB_PASSWORD 的
  局域网/容器部署,反代是这类部署的常见配套。
- 影响:纯可用性(数据先完整刷盘再退出,不丢数据);Docker 里进程退出通常触发
  restart policy 重启。但"陌生人能随时关你服务"对多用户形态不可接受。
- 修复方向:请求头含 X-Forwarded-For / X-Real-IP / Forwarded 时拒绝(经代理的请求
  一定不是本机 Tauri 壳发的,三行);或 webAuthEnabled 时额外要求有效 token、未启用
  鉴权时维持回环判定。前者更简单且不动壳侧契约。

### R5-2(P3)反向代理部署的两处摩擦:SSE 缓冲 + Origin/Host 校验

- ①SSE 缓冲:openSse(api/sse.ts:142-148)、provider/test/stream、update/download 三处
  text/event-stream 响应都没有 X-Accel-Buffering: no 头。nginx 默认 proxy_buffering on,
  事件被攒进缓冲区批量放行——流式输出表现为"卡住然后一次性倾倒",与我们 33ms 合并
  广播的丝滑目标背道而驰。加一个响应头即可让 nginx 自动关缓冲,零配置成本。
- ②Origin 校验:server.ts:102-103 允许"Origin 与请求 Host 完全一致"。nginx 默认
  proxy_pass 会把 Host 改写为上游地址(127.0.0.1:8080),浏览器 Origin 是公网域名,
  两者不等→所有带 Origin 的请求(全部 POST/DELETE)一律 403。需要用户配
  proxy_set_header Host $host 才能用——目前 README 无任何反代说明,用户只会看到
  "网页打得开但什么都保存不了"的灵异现象。
- 修复方向:①代码加响应头(与 R5-1 同批);②README 部署段补一节反代示例
  (Host 透传 + WebSocket upgrade + proxy_buffering off 兜底)。

### R5-3(P3)provider 编辑后 testPassed 永不失效,与 search service 行为不一致【已修复】

- 证据:settings/provider POST(settings.ts:633-634)testPassed: item.testPassed===true
  ? true : body.testPassed——一旦测过,后续把 apiKey/baseUrl 改成什么都保持"已验证"。
  对照 search/service/detail(settings.ts:525-535)明确做了"鉴权字段变更即撤销
  testPassed"。provider 的凭据/端点变更同样使旧测试结论失效,UI 的"已验证"徽章
  变成过期信任指示。
- 修复方向:对齐 search service——apiKey/baseUrl/type 任一变更即清 testPassed。
- 【修复记录 2026-07-27,二次迭代】①settings/provider POST 增加失效判定:type/apiKey/
  baseUrl/chatCompletionsPath/useResponseApi 任一变更 → testPassed=false、testPassedAt=0;
  未变更保持既有"测过即保留"语义(无关字段编辑不抖掉测试状态)。②自查补漏——测试
  飞行竞态:测试跑几秒,期间用户改凭据保存(失效生效),旧配置的测试完成后回来盖章,
  新配置凭空变"已验证"。markProviderTestResult 落章前用 providerAuthChanged(共享判定,
  提取到 model-providers/checks.ts)复核当前配置与被测快照,变了不盖章;
  settings/search/service/test 同型竞态同样补了落章前复核(字段集与 detail 失效规则一致)。

### R5-4(P3)MCP 服务器写端点的并发写窗口(await 网络后整字段替换)【已修复】

- 证据:settings/mcp-server/detail(settings.ts:333-335)与 mcp-server/sync(396-398)
  都在 await syncMcpServerTools(网络往返,秒级)之后才 upsertById + updateSettings。
  窗口内若另一个 mcp-server 写请求先落地(双窗口/SharedWorker 多标签页下完全可能),
  后完成的整对象覆盖会吃掉先写的改动(如工具开关)。跨字段安全(state.settings 在
  await 后现读,只有 mcpServers 数组内同 id 对象互相覆盖)。
- 影响面:窄——需要同一 MCP 服务器在秒级窗口内被写两次;丢失的是配置不是数据。
- 修复方向:sync 返回后按 id 现读最新对象、只合并 commonOptions.tools/connected/
  lastSync* 等同步产物字段,不整对象回写。修复批次里顺手做。
- 【修复记录 2026-07-27】比原方向更彻底:加 per-id 写锁(withMcpServerWriteLock,
  promise 队列),detail/sync/delete 三个写端点对同一服务器严格串行——不仅关掉
  "后完成者覆盖先落地改动"的窗口,也顺带消灭"sync 期间 DELETE 被 sync 完成后的
  upsert 复活"的次生问题。不同 id 之间不互斥(各写各的,await 后现读现改本就安全);
  Map 尺寸以 MCP 服务器数为上界。

### R5-5(P3)update/download 信任面三处可收紧

- ①主机白名单 pub-[a-f0-9]+\.r2\.dev(update.ts:176)匹配【任何人】的 R2 公共桶,
  不只我们的镜像桶;配合 probeCachedInstaller"任意文件名含版本号的 .exe 都算缓存
  安装器"(updates/index.ts:78-85),已授权客户端可诱导下载任意 exe 并在下次检查
  更新时被当作"直接安装"候选。攻击前提是已过鉴权(此时能干的坏事更多),故 P3
  而非 P2——但白名单收紧为 UPDATE_R2_BASE 的精确 host 是零成本的。
- ②下载无签名/哈希校验,信任链完全依赖 HTTPS + 主机白名单。个人项目常态,接受;
  release 流程若日后产 sha256,前端可顺带校验。
- ③下载流无 cancel 处理:客户端断开后 fetch/写盘循环靠 enqueue 抛错间接终止,
  Bun.file writer 不走 end(),留半截文件+句柄直到 GC。加 ReadableStream cancel
  回调 abort fetch + end writer 即可。
- 修复方向:①精确匹配镜像 host;③补 cancel。均为几行。

### R5-6(P3)两处输入边界小洞:ASR WebSocket 裸 JSON.parse + keybindings 的 in 检查

- ①server.ts:176 ws message 里 JSON.parse(data||"{}") 无 try/catch,恶意/损坏文本帧
  直接抛进 uncaughtException 安全网(进程不死但每帧一条错误中心记录)。包一层即可。
- ②settings/keybindings(settings.ts:47)用 body.action in defaults 校验——in 含原型链,
  "__proto__"/"constructor"/"toString" 都能过检:__proto__ 会把 current 对象的原型换成
  请求体(不污染 Object.prototype,但本次写入静默丢失),constructor 等则以垃圾键
  持久化进 settings.keybindings。改 Object.hasOwn 一行修。

### R3-9 处置(部署面评估结论)

- scrape_web SSRF:桌面形态是用户自己的机器,风险自担,与安卓一致【不加运行时
  黑名单】(沿用用户既有决策);Docker/公网形态的真实暴露点是"提示词注入诱导模型
  探测内网/云元数据地址"。处置:README Docker 段加一句安全注记(与 R5-2 的反代
  文档同批),不改代码。
- custom_js 无沙箱:配置它本就等于"在服务进程里跑自己的脚本",与安卓 QuickJS 的
  差异记录在案即可,不在本轮引入沙箱。

### 阶段五其余结论(核对无恙,记录以免重查)

- 鉴权覆盖面矩阵:全部 /api/*(含 /api/events SSE、ASR WebSocket 升级、文件/字体)
  都在 isWebAuthAuthorized 之后;唯二前置例外是 auth/token(换 token 入口,设计使然)
  与 shutdown(见 R5-1)。静态资源不鉴权是有意的(SPA 壳必须先加载才能弹密码闸门),
  且构建产物无敏感内容。EventSource/img/WebSocket 无法设 header 的场景有 access_token
  query 兜底,token 是 HMAC 无状态签名、改密码即全体失效,爆破有全局 5 次/60s 节流
  +恒定延迟。设计成熟。
- 静态文件路径穿越:URL 解析器会归一化 ../ 与反斜杠,残留的 %2e%2e 以字面量参与
  resolve;绝对路径(如 /C:/...)resolve 后前缀检查不过,落 SPA fallback。安全。
  (startsWith 无分隔符的前缀检查理论上偏松,但在 .. 不可达的前提下无可利用面。)
- 记忆域:pending 队列 await 落盘+去重+100 容量上限、损坏文件改名隔离+上报、
  nextMemoryId 启动 recompute 兜底、批量编辑 schema+id 唯一性校验+.bak,写走串行队列。
  本轮最干净的模块之一,无发现。
- 错误中心:环形 200 条+30s 风暴合并+广播失败不反噬;进程安全网最早安装。无发现。
- 更新检查:redirect 探测(免 rate limit)→API 详情→命名约定预测的三级降级,网络
  全挂时 502 带人话提示;Linux 原地更新的 EXDEV/ETXTBSY 双约束处理(copy 到同文件
  系统再 rename)正确;容器内禁用原地更新、提示 docker pull。无发现(R5-5 除外)。
- Dockerfile:distroless + 显式打包 unzip/zip 及其 so 依赖;VOLUME /app/pc-data 与
  dataDir 默认值吻合;exec 形式 ENTRYPOINT 让 SIGTERM 直达进程,优雅停机刷盘链
  (state.json→活库脏行→生成中会话 reconcile→WAL checkpoint→释放锁)完整。容器内
  端口固定不跳、更新检查返回 containerized 由前端引导 docker pull。健康。
- Linux 大小写敏感:未发现任何大小写不一致的路径引用(附件/字体/静态资源均用
  readdir 原样名或内容 hash 名)。
- 版本脚本:bump-version 四处同步+写后立即用 CI 同款校验复验;check-version-sync
  四处一致性红灯。无发现。
- 顺手修复:server.ts:36 Linux 缺依赖提示仍写着"large DOCX streaming extract"——
  该外部依赖已在 R4-3 修复中删除,文案改为"backup restore from ZIP"(unzip 现存
  唯一调用点是备份导入,skill ZIP 导入走内存 readZipEntries)。
- 小结:本阶段无 P0/P1。横切基建(鉴权/SSE/错误中心/记忆/更新)是全仓质量上限
  水准;发现集中在"反向代理部署"这一未被文档与测试覆盖的形态(R5-1/R5-2),
  以及若干几行即修的边界小洞(R5-3~R5-6)。
- 【追记 2026-07-27】用户指出 R5-3/R5-4/R1-12 属用户高频感知问题,已当场修复;应
  "是否足够终极"的追问自查出两处残余缝并二次迭代:R1-12 改设置内墓碑(第一版迁移
  标记不随备份走)、R5-3 补测试飞行竞态的落章前复核(见各条修复记录)。R5-4 的
  per-id 写锁复查无残余缝(工具运行时对 ssePostEndpoint 的就地写属 R3 已记录的独立
  问题,与锁无关)。顺手:backup/import.ts 里作哈希分隔符的裸 NUL 字节改写为 \u0000
  转义(行为不变)——裸字节让 rg 把整个文件当二进制跳过,本次差点漏查,属工具链
  卫生问题。194 服务端测试全过(新增墓碑回归 4 用例),tsc 干净。剩余待修:
  R5-1/R5-2(反代)、R5-5/R5-6(边界小洞),归入修复批次。

## 阶段六:前端基座与数据流

审查范围:root.tsx、services/(api|app-events)、workers/app-events-worker、stores/*(含
slices/hooks)、hooks/*、i18n.ts+locales、types/*(与服务端 DTO 镜像),约 2.9k 行,全部逐行读过。

### R6-1(P2)SSE 通道无客户端活性看门狗——睡眠恢复后事件通道静默死亡

- 位置:`web-ui/app/services/api.ts` sse()、`web-ui/app/workers/app-events-worker.ts` run()。
- 服务端每 15s 发 `: heartbeat` 注释帧(sse.ts:126),但两层客户端都不消费它:没有
  "N 秒无字节即判死"的看门狗,reader.read() 无超时(timeout:false / 裸 fetch)。
- 后果:笔记本合盖睡眠/切换 Wi-Fi/VPN 断开产生半开 TCP 时,read() 永久挂起——不抛错、
  不触发重连,退避逻辑再完善也进不去。/api/events(设置/记忆/错误/列表失效)与会话详情
  流(conversations.tsx:474 同走 sse())一起静默死亡:设置改了不同步、列表不刷新、
  生成中的会话恢复后永远卡"生成中",直到用户手动刷新页面。桌面应用合盖睡眠是日常
  高频场景,故 P2。
- 修复方向:两处读循环记录 lastByteAt(心跳注释帧也算字节),45s(3×服务端心跳,与
  worker 判活口径一致)无字节 → abort 当前连接走既有重连路径。服务端心跳已备好,
  纯客户端改动。

### R6-2(P3)SharedWorker 失败面只兜构造异常,异步加载失败无回退

- 位置:`web-ui/app/services/app-events.ts` startViaSharedWorker()。
- try/catch 只兜 `new SharedWorker()` 同步抛错;worker 脚本异步加载/求值失败(个别
  WebView 支持 SharedWorker 但不支持 module worker、脚本 404 等)时 `worker.onerror`
  未监听 → 返回 true 但通道永远无消息,不回退页内直连,事件面静默死亡。
- 修复方向:接 worker.onerror,未收到任何消息前出错 → 一次性切 startDirect()。

### R6-3(P3)密码闸门:弹窗已开时重复 401 事件清空正在输入的密码

- 位置:`web-ui/app/components/web-auth-gate.tsx`:18-25。
- onWebAuthRequired 处理器无条件 setPassword("")/setError(null):用户正在输密码时,
  任何后台请求再触发一次 401(事件按请求数分发,不去重)都会清空输入框。
- 修复方向:open 已为 true 时只保持弹窗,跳过字段重置(两行改动)。

### R6-4(P3)unhandledrejection 兜底不过滤 AbortError、本地错误无风暴合并

- 位置:`web-ui/app/root.tsx`:208-224。
- 一律 toast 原始 message:导航/组件卸载取消请求产生的 AbortError 类拒绝会弹无意义
  toast;且 reportLocalError 无 30s 同 message 合并(后端错误中心有,本地聚合没有),
  循环失败场景 N 个拒绝 = N 个 toast + N 条目。
- 修复方向:AbortError 直接忽略;本地聚合复用"同 domain+message 30s 合并计数"语义。

### R6-5(P3,记录)types/settings.ts 手写镜像服务端 Settings,存在漂移面

- `types/dto.ts` 已是 @server type-only re-export(编译期单源,好样板);但
  `types/settings.ts`(383 行)是手写镜像——服务端加字段前端类型不知情(本次
  dismissedSearchServiceTypes 即例)。当前无运行时风险(前端不整体回写 settings,
  各端点服务端都 spread 保留),但属可消除的漂移面。改进方向:阶段八设置域审查时
  评估仿 dto.ts 单源化的可行性(前端是否依赖与服务端不同的宽松形状)。

### 阶段六体检正常项(已核,后续不必重查)

- **DTO 镜像**:dto.ts 编译期单源(@server re-export),字段增删必然两端同步,零漂移。
- **i18n**:6 命名空间 zh/en 各 1299 键逐一比对零差异;资源随包打进 bundle 无网络加载;
  fallbackLng zh-CN;语言选择 localStorage 持久化且 <html lang> 跟随。
- **SSE 重连纪律**:两层均指数退避 1s→30s、收完整帧复位计数;4xx(除 408/429)判不可
  自愈即停(401 交密码闸门,解锁后整页 reload);服务端连接即推全量快照,重连即状态补偿。
- **连接预算**:SharedWorker 全浏览器共享 1 条 /api/events,页面 15s ping+pagehide bye
  +45s 判死摘除+bfcache 复活重纳,协议自洽;不支持时构造抛错回退页内直连(缺口见 R6-2)。
- **401 风暴面**:sse/worker 均 401 即停不空转;ky 层 401 清 token+单事件弹闸门,
  auth/token 端点豁免防死循环;token 过期带 10s skew 提前判废。
- **stores**:zustand 单 store 三 slice,极简;草稿仅内存、会话粒度,clearDraft 删键,
  无持久化无泄漏;确认框 Promise 化新请求挤旧请求按取消落定,无悬挂。
- **错误中心契约**:快照只入 store 不 toast(重连不重复打扰),增量才路由 toast,
  info 级静默;风暴抑制主责在后端 30s 合并(本地聚合缺口见 R6-4)。
- **列表 hook**:请求 epoch 守卫防乱序回写;invalidate 250ms 去抖;刷新保已加载窗口
  (上限 100)不缩列表;卸载清定时器。
- **hotkeys**:capture 阶段抢默认行为;输入框聚焦只放行带修饰键组合;IME composing
  放行;wheel passive:false 才能压 WebView2 原生缩放;ref 持最新配置不重建监听器。
- **clockOffset**:单一写点(会话流 serverTime)单一读点(lib/utils serverNow),无滥用。

### 阶段六小结

前端基座整体质量高:连接预算、快照重放、故障隔离、epoch 守卫这些纪律都落实了,注释
把契约写得很清楚。系统性缺口只有一个——整条 SSE 体系(两层实现)都没有活性看门狗
(R6-1,P2),这是"睡眠恢复后界面假死"这类用户日常可感知问题的根源,服务端心跳
基础设施已就绪,修复成本低收益大。其余为回退缺口与打扰性小疵(R6-2/3/4)和一处
类型漂移面记录(R6-5)。

## 阶段七:前端会话主界面

审查范围:routes/conversations.tsx(2104 行)、components/message/*(chat-message 1162、
message-part、chain-of-thought、parts/*)、components/input/chat-input.tsx(954)、
components/markdown/*(markdown 244、code-block 795)、conversation-sidebar.tsx(1329)、
conversation-search-button、conversation-quick-jump 等,合计约 8k 行,逐行读过。

### R7-1(P1·安全)Markdown 渲染绕过 Streamdown 的消毒/加固管线,且无 CSP 兜底

- 位置:`web-ui/app/components/markdown/markdown.tsx`:233 `rehypePlugins={[rehypeKatex, rehypeRaw]}`;
  `web-ui/src-tauri/tauri.conf.json`:36 `"csp": null` + `"dangerousDisableAssetCspModification": true`。
- 根因:Streamdown 默认 rehype 管线是 `[raw, sanitize(defaultSchema), harden]`——先解析原始
  HTML,再消毒(限定标签/属性/协议),再 harden(限定 img/link 前缀)。这是它专为
  "渲染不可信 LLM 输出"内置的防线。本项目显式传入 `rehypePlugins`,把整条默认管线整个
  替换成只有 `[rehypeKatex, rehypeRaw]`:rehypeRaw 照常把正文里的原始 HTML 解析成节点,
  但 sanitize 和 harden 都没了。加上 Tauri 侧 CSP 完全关闭(csp:null 且显式禁用自动注入),
  没有任何兜底。
- 数据流:所有消息正文(text-part.tsx → Markdown)、翻译块、被搜索结果影响的模型输出、
  以及**跨端备份/分享导入的会话**都经此渲染。不可信内容可注入原始 HTML:
  `<img src=x onerror=...>`、`<iframe>`、`javascript:` 链接、外链信标(数据外泄)。在无 CSP 的
  WebView 里危害实际;web 托管(密码)模式下同样成立。项目威胁模型明确包含"PC↔APP 备份
  互通/会话分享",一条别人构造的会话在被查看时即触发,故评 P1。
- 修复方向(需保证数学公式与 citation 徽章仍渲染):① 首选不再覆盖 rehypePlugins,改用
  Streamdown 默认(已含 raw+sanitize+harden),数学走它的 plugins.math / remark 选项接入;
  或 ② 保留 rehypeRaw 但在其后补 `rehype-sanitize`,schema 白名单放行 KaTeX/MathML 产物
  (span/svg/annotation/math 及其 class/style)。改完务必回归:块/行内公式、代码块、citation
  徽章、表格。另建议给 Tauri 配一份最小 CSP 作纵深防御。
- 备注:settings/data.tsx:714、extensions.tsx:1802 的 dangerouslySetInnerHTML 注入的是 i18n
  静态资源串(可信),非漏洞,但属可收敛的用法。

### R7-2(P3)模型调用错误横幅用正则扫正文,正常内容会误判

- 位置:`chat-message.tsx`:69-77 `messageHasModelCallError`。
- 对 ASSISTANT 正文做 `/请求失败|request failed|timeout|401|403|404|429|500|.../i` 正则匹配,
  命中就在消息下方挂"打开模型设置"横幅。任何正常讨论 HTTP 状态码/超时/报错文案的回答
  (例如解释"500 错误怎么排查")都会误触发横幅。误报型 UX 瑕疵。
- 修复方向:错误态应由结构化信号(part/annotation 上的 error 标记或消息级 status)驱动,
  而非扫正文关键词。

### R7-3(P3)长会话轮询兜底的"单调守卫"以序列化长度作进度代理,边缘会拒真更新

- 位置:`conversations.tsx`:553-567(useConversationDetail 的 10s 兜底轮询)。
- 守卫逻辑:节点数相同则比较最后一个节点 `JSON.stringify(parts).length`,更短即判旧数据
  丢弃。若一次合法编辑把末条消息改短(用户改成更短文本、或分支切换到更短候选),轮询响应
  会被误当陈旧丢弃。因 SSE 是主路径、下一帧即纠正,影响短暂,故 P3。
- 修复方向:用版本号/updateAt 单调量替代"序列化长度"这一脆弱代理。

### R7-4(P3)长耗时操作 timeout:false 且无取消,压缩对话对话框无法中途关闭

- 位置:translate(:1419)、compress(:1615)、regenerate-title(:1506)均 `{ timeout: false }`;
  压缩对话框 `onOpenChange={(open)=>!compressing && ...}` 压缩期间锁死。
- 后端若卡住,前端 spinner 无限转、无客户端 abort、压缩框还关不掉,用户只能刷新整页。
- 修复方向:给这些请求接 AbortController + 合理上限(或后端保证有界),对话框提供取消。

### 阶段七体检正常项(已核,后续不必重查)

- **零加载态切换**:detailCache LRU(20)+ render 阶段派生 state(renderedForId!==activeId 同步
  换内容),回访会话首帧即缓存内容,SSE 快照静默校正;删除/批删/移动会话显式清缓存。
- **SSE 消费**:mounted + abortController 双守卫;snapshot/node_update 分派清晰,
  applyNodeUpdate 返回同引用即跳过;prev 为空时 queueMicrotask 触发 refreshNonce 重订阅;
  致命 4xx(非 404)呈错误、404 归"暂无会话",可自愈错误交 sse() 内建重连。
- **渲染性能**:Virtuoso 虚拟化 + initialTopMostItemIndex 首帧即定位(无"中部→跳末尾");
  入场动画只施于"进入会话后新出现"的消息(knownIdsRef 锁历史集),长会话首屏不并发 N 动画;
  code-block 用 shiki token 化渲染(非 innerHTML)+ MAX_SHIKI_CODE_LENGTH 超长降级 + 异步
  高亮订阅缓存与取消;Markdown 自定义 components 提到 useMemo 稳定引用防流式重建。
- **渲染边界隔离**:ChatInputArea 用 React.memo 把"草稿内容订阅"下沉,打字只重渲染输入区,
  侧边栏/顶栏/对话框/消息列表全不动(useDraftInputController 刻意不订阅草稿内容)。
- **消息动作**:regenerate/delete/fork/branch 全部 try/catch + toast 失败提示(此前静默复原);
  USER 消息重生成/删除走 confirmDialog;TTS 用 app 级 chunk 控制器,行卸载不拆(虚拟列表
  滚出/切会话不打断播放),仅停本行的 legacy 单块播放。
- **分享选择态**:activeId 变化重置选择;全选/反选/计数;导出对话框按选中集过滤。
- **搜索**:250ms 去抖 + requestId 竞态守卫;命中带 ?msg= 定位,遍历所有分支找该消息所在
  node,递增延迟重试跨过 Virtuoso 布局稳定窗口,定位期间不 followOutput 防被拉到底。
- **输入区**:ASR WebSocket token 走 access_token query(无法带 header),完整资源释放链
  (processor/source/context/stream/socket)+ 组件卸载 stopAsr;粘贴长文转文件;拖拽调高持久化
  松手才 POST;优化提示词 60s 超时 + 8s 慢提示 + 撤销原文;发送失败草稿保留(clearDraft 在
  await 成功之后)。
- **侧边栏**:分组(置顶/按日)、多选批删、改名/移动/重生成标题均 confirm+toast;按 assistant
  过滤;回到顶部;profile 本地乐观 + refreshSettingsStore 校正。
- **快捷键接入**:hotkeyHandlerRef 每 render 更新闭包、useEffect 只挂一次,无陈旧无重建。

### 阶段七小结

会话主界面是全项目工程密度最高的部分,零加载态切换、虚拟化定位、渲染边界隔离、SSE/轮询
双守卫这些都做得很扎实,注释把每个反直觉决策的来由都写清楚了。唯一的重大问题是 R7-1:
Markdown 渲染把 Streamdown 专为不可信 LLM 输出内置的消毒/加固防线整条替换掉,叠加 Tauri
CSP 全关,构成一条从"消息正文/翻译/导入会话"到原始 HTML 注入的现实攻击面——在本项目
"跨端备份互通、会话分享"的威胁模型下应尽快修复(建议进最近一个修复批次,甚至优先于其他
P3)。其余 R7-2/3/4 为误报与打扰性小疵。

## 阶段八:前端设置域与其余页面

审查范围:`routes/settings.tsx`(230 行壳)+ `components/settings/*`(约 9.7k 行:providers 1394、
extensions 1952、data 1264、speech 1106、assistants 916、search 737、proxy 490、default-models 415、
general 294、stats/logs/app-errors/about/shared)+ `components/memory/*`(631 行)+ `routes/images.tsx`
(476 行)。逐文件读了壳/数据域/供应商域全文,其余按操作面(删除/保存/网络)定向核查。

### R8-1(P2)七处破坏性删除无确认门控,一次误点即永久丢失用户数据

- 全项目删除操作的既定规范是 confirmDialog 门控(会话删除/批删、assistant 两段确认、MCP server、
  skill、搜索服务、WebDAV/S3 备份删除都有),但以下 7 处是"点击垃圾桶图标即直删",无确认、无撤销:
  - `extensions.tsx`:1177 **lorebook 删除**——世界书可承载大量用户创作条目,损失最重;
  - `extensions.tsx`:1325 快捷消息删除;
  - `extensions.tsx`:1570 注入模板(deletePath 泛型编辑器)删除;
  - `speech.tsx`:303 TTS 供应商删除、:849 ASR 供应商删除(连带 API Key/音色配置);
  - `memory/memory-section.tsx`:46 **记忆条目删除**——AI 积累或用户手写的记忆,单击即没;
  - `routes/images.tsx`:454 生成图删除。
- 这些按钮多为 destructive/ghost 小图标,紧邻其他操作图标,误触概率不低。按"用户数据千万不能
  丢"的红线,评 P2。
- 修复方向:统一补 confirmDialog(danger)。lorebook/记忆这类内容型实体必须加;供应商/生成图
  至少加轻量确认,或提供删除后撤销(toast undo)。

### R8-2(P2)"保存窗口内键击丢失"竞态只在 extensions.tsx 修了,其余 5 个自动保存分区仍带病

- 这是项目里已确诊并修复过一次的 bug 类(McpServerEditor 注释原话:"URL input eats characters")。
  修复三件套 = savingRef + editedDuringSaveRef + 重对齐 effect 只依赖 selectedId(serversRef 读最新)。
  但只落在 `extensions.tsx`(savingRef 16 处);同模式的其余分区均未同步:
  - `providers.tsx`/`assistants.tsx`/`search.tsx`/`proxy.tsx`:dirtyRef 自动保存,save 完成回调
    无条件 `dirtyRef=false`。若键击落在"保存发起→resolve"窗口内,该键击的 dirty 标记被抹掉,
    下一轮 700ms 定时器一看 dirty=false 就跳过——**字符显示在框里、提示"已自动保存",实际
    永不落盘**,切页即丢。
  - `data.tsx` 最重(双倍病):WebDAV/S3 两套草稿除 dirty 抹除外,重对齐 effect(:122/:321)直接
    依赖 settings 派生的字段值——每次 autosave→onSettings 回环都会重触发,把保存窗口内新敲的
    字符**当场可见地清掉**(正是 extensions 修复注释里点名的旧病根)。
- 本机部署 RTT 小、窗口窄;Docker/反代远程部署 RTT 变大后高频复现(连续输入 >700ms 即可触发)。
- 修复方向:把 extensions.tsx 的三件套抽成 useAutosaveDraft 共享 hook,5 个分区统一换装,顺带
  消掉五份重复的 debounce/dirty 样板(冗余代码清理)。

### R8-3(P3)助手正则每次渲染重编译,恶意/病态正则可冻结 UI 线程

- `lib/assistant-regex.ts`:38 `new RegExp(findRegex,"g")` 逐条现编译、无缓存,消息每个 text part
  每次渲染都跑全量正则(流式期间逐帧)。
- 正则来自 assistant 配置——本人手写风险低,但经备份导入/分享的助手可携带灾难性回溯正则
  (ReDoS),一条 `(a+)+$` 式 pattern 即可把渲染线程卡死,和 R7-1 同属"导入内容不可信"威胁面。
- 修复方向:按 findRegex 字符串做模块级 RegExp 缓存;评估用 RE2 类线性引擎或长度上限兜底。

### R8-4(P3)R6-5 结论:settings 手写类型镜像为编译期风险,运行期安全

- 各分区更新一律 `onSettings({...settings, 字段})` 展开旧对象,服务端新增的未知字段会原样带回,
  不存在运行期字段丢失;`types/settings.ts` 手写镜像只构成编译期漂移(前端看不见新字段、类型
  骗人)。修复方向:像 `types/dto.ts` 一样从 `pc-server` 类型 re-export 单源化。

### 阶段八体检正常项(已核,后续不必重查)

- **数据域(data.tsx,数据安全关键路径)**:导入走 XHR octet-stream 流式直传(10GB 级备份不炸堆、
  有上传进度、X-Filename 编码),导出走 XHR onprogress + 服务端 X-Export-Filename;导入/恢复/
  备份删除全 confirm 门控;custom_js 等安全告警 15s 长驻 toast;WebDAV/S3 备份/恢复走 SSE 进度
  流(consumeBackupSse 解析 progress/done/error);安卓 schema 注册状态与引导完整。
- **providers.tsx**:draft+700ms 自动保存,重对齐 effect 只依赖 selectedId + providersRef 防回环
  clobber(R8-2 的"重对齐"半件套已有,缺 savingRef 半件套);?providerId= 深链只首挂生效不抢
  用户点击;手动模型内存缓存(toggle off 不消失)、fetched 与 draft 合并去重、按过滤集全选;
  模型类型按 id 推断(embedding/image 正则)可覆盖。
- **assistants.tsx**:删除两段确认(先删助手、再问记忆去留,取消则留孤儿记忆可管理);预设消息
  增删改;正则条目编辑。
- **settings 壳**:本地乐观镜像 + streamedSettings SSE 校正;?section= 深链校验后才 setSection;
  logs/stats 懒加载(切到该分区才拉);清日志 confirm。
- **proxy.tsx**:测试 URL 存 localStorage 不进备份(纯 UI 偏好,注释講明理由);端口改动走
  settings/port 显式保存。
- **search.tsx**:服务删除 confirm;R1-12 墓碑修复后的内置服务管理已在阶段一/五核过。
- **images.tsx**:生成/编辑双模式、文件上传走 postMultipart、失败 toast;唯一问题是删除无确认
  (归入 R8-1)。
- **memory 域**:全局/助手记忆增改查走乐观 + SSE 校正;唯一问题是删除无确认(归入 R8-1)。
- 设置域无 eval/new Function;两处 dangerouslySetInnerHTML 皆为 i18n 资源串(可信源)。

### 阶段八小结

设置域整体延续了主界面的工程水准:数据域的流式导入导出、providers 的深链/合并/缓存细节、
extensions 的竞态注释都属上乘。两个 P2 都是"规范已立但未铺满"的一致性缺口:删除确认门控
(R8-1,7 处漏网)与保存竞态三件套(R8-2,5 个分区未同步)——后者尤其说明修 bug 时应检索
同模式的所有落点。R8-3/4 为低危记录项。

## 阶段九:综合诊断与修复批次规划

完成于 2026-07-27。输入:阶段一~八共 56 条编号发现(R1×13、R2×8、R3×10、R4×6、R5×6、
R6×5、R7×4、R8×4)。其中审查期间已当场修复 7 条(R1-12、R2-1+R2-7、R4-3、R4-4、R5-3、
R5-4),纯记录/无需行动 5 条(R1-5 并入 R2-4、R2-8、R3-8、R3-9 已决策、R6-5 并入 R8-4),
待修 44 条:**P1×4、P2×14、P3×26**。

### 总结论:架构是否已达"成熟商业应用"标准

**骨架已达标,缝隙未达标。** 各阶段小结一致指向同一个画像:
- 达标面:显式 bootstrap 编排、实例锁、原子写+恢复梯队、DB-first 会话域+working-set、
  连接预算(单 SSE 通道+SharedWorker)、错误中心、鉴权体系(HMAC/timing-safe/限速)、
  备份互操作三轮加固、前端零加载态/虚拟化/渲染边界隔离——这些主干都是商业级的,
  且注释纪律极好(反直觉决策都有出处)。
- 未达标面集中在三类"缝":
  ① **规范已立但未铺满**(同一 bug 类修了一处漏了同模式的其余落点):R8-2 保存竞态
    只修 extensions、R8-1 删除确认 7 处漏网、R3-1/R3-4 超时与 abort 纪律 OpenAI 有
    Claude/Google 无、R4-1 备份标记补了 memory 漏了 conversations;
  ② **生命周期无人负责的缓存/快照**:R1-2 recovery 残留、R1-13 化石快照与 updates/
    堆积、R3-3 ssePostEndpoint 持久化脏缓存;
  ③ **未被文档与测试覆盖的部署形态**:反代(R5-1/R5-2)、睡眠恢复(R6-1)、
    升级重迁移(R1-1)。
  这三类都是系统性工程习惯问题,不是架构问题——修复批次按此归因组织,每批修一类根因
  的全部落点,并以"检索同模式所有落点"为完成标准。

### 修复批次规划(每批可独立验证,按 严重度×影响面 排序)

**批次一:数据安全红线(P1×2+P2×2)——最先做**
- R4-1 恢复备份补写 CONVERSATIONS_SQLITE_MIGRATION 标记(一行级)+方案 B 灌库前 reportError;
- R4-2 恢复失败回滚:try/catch 清暂存 conversations + 从 pre-import.bak 回滚 setState;
- R1-2 恢复链按 mtime 统一排序取最新 + 成功落盘删 recovery-* + applied-*/超龄纳入清扫
  + 恢复时上报所用备份日期;
- R8-1 七处删除补 confirmDialog(lorebook/记忆为必须,供应商/生成图/快捷消息/模板同批)。
- 验证:恢复演练脚本(老用户带 pre-sqlite.bak 恢复→重启无化石复活;恢复中途失败→回滚
  干净);删除路径手测+单测。

**批次二:安全加固(P1×1+P2×2+P3×3)**
- R7-1 Markdown 消毒:恢复 Streamdown 默认管线或 rehypeRaw 后补 rehype-sanitize
  (白名单放行 KaTeX/citation 产物)+ Tauri 最小 CSP;回归:公式/代码块/citation/表格;
- R5-1 shutdown 端点:带 X-Forwarded-For/X-Real-IP/Forwarded 头即拒(或 webAuth 时要 token);
- R1-3 静态路径前缀校验补分隔符;
- R5-6 ASR WS JSON.parse 包 try/catch(=R1-10)+ keybindings 改 Object.hasOwn;
- R5-5 update 主机白名单收紧为精确 host + 下载流补 cancel 回调;
- R8-3 assistant-regex 模块级 RegExp 缓存 + 病态正则防线(长度上限/超时降级)。
- 验证:XSS payload 集回归、反代 curl 演练、恶意帧/键名单测。

**批次三:挂死与看门狗(P2×5+P3×3)——"无人值守可用性"**
- R3-1 Claude/Google 主链路补头超时+空闲看门狗(下沉 tool-loop 骨架统一);
- R3-4 工具执行循环每迭代查 signal.aborted(三家统一,删 abortCheckAfterRead 冻结点;
  R4-5 随之自愈);
- R3-5 超时文案/注释更正(随 R3-1 消亡);
- R3-2 MCP SSE 握手 1s race 改 tick-continue,以 15s 总预算为准 + clearTimeout;
- R3-3 ssePostEndpoint 迁出 settings 改内存缓存,失败重试清缓存重握手 + 旧值一次性清理;
- R3-10 MCP tools/call 超时放宽(300s 或按服务器可配);
- R6-1 两层 SSE 读循环加 45s 无字节看门狗(心跳帧计数,超时 abort 走既有重连);
- R7-4 translate/compress/regenerate-title 接 AbortController+上限,压缩对话框可取消。
- 验证:黑洞上游模拟(握手后不发数据)、合盖睡眠/断网恢复手测、MCP 慢服务器模拟。

**批次四:升级与启动韧性(P1×1+P2×2)——设计工作量最大**
- R1-1 服务端"先绑端口再迁移"(迁移期 API 503+前端迁移进度页)+ 壳超时改"子进程
  存活即继续等"+ 大迁移分批提交;
- R1-4 壳按退出码/标记行分诊启动失败文案(兑现或删除 port_in_use 死契约);
- R1-13 数据目录卫生(需用户确认保留策略):recovery 成功即删、corrupt/applied 保最新
  +30 天清、updates/ 删 ≤当前版本、化石快照健康门槛退役、备份类收敛 backups/ 子目录、
  孤儿附件 GC 决策(与安卓语义对齐问题)。
- 验证:大数据量老版本升级演练(人造 GB 级 files/+pre-1.2.6 state)、双开/孤儿进程演练。

**批次五:前端交互一致性(P2×1+P3×5)**
- R8-2 抽 useAutosaveDraft 共享 hook(savingRef 三件套),providers/assistants/search/
  proxy/data 五分区换装,消五份样板;
- R6-3 密码闸门弹窗开着时跳过字段重置;
- R6-4 unhandledrejection 过滤 AbortError + 本地 30s 风暴合并;
- R6-2 SharedWorker onerror 回退直连;
- R7-2 模型错误横幅改结构化信号驱动(服务端在消息/part 上落 error 标记);
- R7-3 轮询单调守卫改版本号/updateAt。
- 验证:高延迟(节流)下连续输入不丢字符;5 分区自动保存回归。

**批次六:会话域竞态与 IO 卫生(P2×2+P3×4)**
- R2-2 fire-and-forget 续体自持 checkout/release(结构性修复 working-set 不变式);
- R2-3 generateAnswer 入口"先 abort 旧流"守卫(或按会话串行化);
- R2-4 删会话/清库时按前缀清 pendingBroadcasts;
- R2-6 删会话逐 controller close 详情 SSE;
- R2-5+R1-6 清无效 saveState() + addLog 改节流保存(同根:DB-first 后遗留调用);
- R3-7 parseSseChunks 多行 data join 后整体 parse(对齐 Claude 读器)。
- 验证:慢 OCR 下切会话/连发消息演练;现有 190+ 测试回归。

**批次七:部署面与杂项(P3×6)**
- R5-2 SSE 响应加 X-Accel-Buffering:no + README 反代示例(Host 透传/upgrade/缓冲)
  + R3-9 的 Docker SSRF 注记(纯文档);
- R1-8 mime 表补 woff2/ttf/json/wasm;
- R1-7 系统代理读取改异步后台刷新;
- R1-11 提示词字数正则改写改一次性迁移(标记门控);
- R4-6 备份 staging 文件名跨平台字符清洗(一行级)。
- 验证:nginx 反代全功能演练(本批完成即可宣布"支持反代部署")。

**批次八:卫生与单源化(P3×4)——收尾**
- R3-6 死代码三处删除;R1-9+R3-5 注释锈蚀更正;
- R8-4(R6-5)types/settings.ts 改 @server type-only re-export 单源化;
- routes/settings.tsx Section 联合类型死成员 "plan" 删除。
- 验证:tsc + 全量测试 + rg 死引用扫描。

**不动(记录在案)**:R2-8(分页全扫,Agent 化前不动)、R3-8(usage 覆盖口径,两端一致)、
R3-9(SSRF/沙箱,已决策跟安卓)、R5-5②(签名校验,等 release 流程产 sha256)。

### 与未来路线的衔接评估

- **多标签页**:SharedWorker 单通道、working-set 单实例注册表、会话详情 LRU 缓存都是
  为此铺的路;批次六(R2-2/R2-3 不变式加固)是多标签下"同会话双开"安全的前置条件。
- **前端 UI/UX 重设计**:渲染边界隔离(输入区 memo 下沉、Virtuoso)使重皮肤不动数据流;
  批次五的 useAutosaveDraft 抽象会让新设置 UI 直接复用。
- **流式上传+逐页解析**:R4-3 修复已落 zip 中央目录随机访问读取器与协作式让出,
  剩"上传入口 formData 整文件进内存"与"Bun Worker+wasm 单 exe 打包验证"两步。
- **Agent 化**:需要 R2-8 的 SQL 化分页、按会话生成队列(批次六 R2-3 的串行化方案
  即其雏形)、以及 R3-10 的 MCP 长任务超时可配(批次三)。

### 排期建议

批次一/二合计改动小、收益最大,建议先行合并验证后出 1.5.1;批次三/四涉及行为与壳,
适合 1.6.0 主题;五~八可穿插。每批完成标准:该批全部条目 + "同模式落点检索"记录 +
对应演练/测试通过 + 发现文档回填修复记录。

---

## 批次一修复记录(2026-07-27)

批次一"数据安全红线"四项全部完成:pc-server 203 测试全绿(新增 1 个 e2e 文件 2 用例、
恢复链回归重写 13 用例),pc-server 与 web-ui tsc 干净。

### R4-1(P1)恢复备份丢迁移标记 → 已修
- backup/import.ts 新增 RESTORE_GUARANTEED_MIGRATIONS + seedRestoreMigrationMarkers():两条 PC 恢复
  路径在 normalizeState **之前**把标记种进 incoming。登记四个:CONVERSATIONS_SQLITE(防化石会话
  复活——本项主目标)、MEMORY_FILE_SPLIT(原有逻辑收编)、FILE_DEDUP(防恢复后下次启动全量
  sha256 重扫,R1-1 放大器)、PROVIDER_REORDER(防 normalizeState 重排备份里用户排好的供应商顺序
  ——审查未记录的衍生问题,顺带修复)。原两处事后补写 MEMORY 标记的重复块已删(冗余清理)。
- 方案 B 灌库前补 reportError(state-load.ts):化石灌库对用户可见,附恢复条数与核对提醒。
- 注释契约:新增一次性迁移时必须评估"恢复后不变式是否成立",成立则登记,不成立则保证幂等重跑。
- 回归:backup/restore-markers-rollback.e2e.test.ts 用例 1(真服务器导入后轮询 state.json 断言四标记齐全)。

### R4-2(P2)恢复失败残留半应用状态 → 已修
- captureRestoreRollbackPoint():进破坏性段前留存内存快照(structuredClone(state) +
  memoryStore.exportFlat()),失败回滚 state+记忆并 saveState/broadcast;回滚自身再失败的最后防线 =
  清暂存 conversations。三条路径全覆盖:applyBackupPayload、applyPcBackupFromExtractDir、安卓合并段
  (原样抽出为 applyAndroidOriginZipFromExtractDir 以获得 try/catch 包裹,行为不变)。
- 活库不需回滚:resetConversationsDbTo 单事务,中途失败即 SQLite 自动回滚;reset 成功后流程再无可
  抛点。附件孤儿字节不回删(数据安全优先,同内容再导入会被去重复用)。
- 回归:e2e 用例 2(毒备份的会话 id 为对象 → 灌库抛错 → 断言设置回滚、state.json 无暂存残留、
  活库基线会话原样、毒会话未入库)。

### R1-2(P1)恢复链错序+残留 → 已修(含终极补强)
- recoverStateFromBackups 重写:recovery-*/daily.bak/pre-sqlite.bak 统一按 mtime 新鲜度排序取最新可
  解析者,同龄按类别破平;返回 {state, source, mtimeMs},loadState 的 reportError 写明所用备份来源
  与落盘时间(R1-2 ④)。
- 终极补强 maybeAdoptFresherRecovery:state.json 完好但磁盘上有更新鲜的 recovery(落盘八连败写出
  recovery 后进程随即退出的时序)→ 启动自动采用并上报。这是审查记录之外的真实丢数据窗口,本批一并堵死。
- 清扫:成功落盘后清全部过期 recovery(armed 机制:首次成功保存清一次 + 写过 recovery 后重新武装,
  避免每次保存 readdir);.applied-*(旧版归档残留,现行代码不再生成)超龄 30 天启动清扫。
- 设计修订:采用/恢复时**不再**改名 .applied 归档——采用后到首次成功落盘之间若再崩溃,改名会让这份
  最新数据永久失联;现为幂等重采用,成功落盘后统一清扫,严格更安全。
- 回归:state-recovery.test.ts 重写:陈旧 recovery 不得压过昨日 daily.bak、采用幂等、两类清扫边界、
  每日滚动备份原有用例保留。

### R8-1(P2)七处删除无确认 → 已修(实际八处)
- 世界书/快捷消息/注入模板(extensions.tsx)、TTS/ASR 服务(speech.tsx)、记忆条目
  (memory-section.tsx)、生成图片(images.tsx)全部补 confirmDialog(danger:true),带名称插值。
- 同模式清扫补获第八处:自定义字体删除(font-picker.tsx)。
- i18n:zh-CN/en-US 各 8 键对齐(settings.json ×7 + common.json image_page ×1),JSON 解析校验通过。
- 复扫:全仓 api.delete 均已直接门控,或属非破坏性(输入框附件移除)/上游已门控(消息、会话删除)。

---

## 批次二修复记录(2026-07-27)

批次二"安全加固"六项全部完成,另同模式补获一处路径穿越。pc-server 213 测试全绿(新增
keybindings 白名单 6 用例、静态 CSP 3 用例、shutdown 反代 1 用例、WS 恶意帧 1 e2e),
web-ui 39 测试全绿(新增 Markdown 消毒渲染回归 11 用例、assistant-regex 防线 4 用例),
两端 tsc 干净,web-ui 生产构建通过。

### R7-1(P1)Markdown 渲染绕过消毒管线 → 已修(方案①,官方口子)
- markdown.tsx 不再覆盖 rehypePlugins,恢复 Streamdown 2.2 默认管线 raw → sanitize
  (defaultSchema)→ harden。数学走官方 plugins.math 口子:remarkPlugin(remark-math)追加
  在 remark 链末,rehypePlugin(rehype-katex)追加在 sanitize/harden **之后**——数学节点以
  <code class="language-math">(defaultSchema 放行 language-* 类)穿过消毒,块级公式由
  pre>code 结构判定 display,KaTeX 产物(span/MathML/内联 style)生成于消毒之后不被剥。
  已核实 mdast-util-math/rehype-katex/hast-util-sanitize 三方源码确认此路径无损。
- remark 默认已含 gfm → remarkPlugins 一并不传;rehype-raw 由默认管线内置提供。
  依赖清理:rehype-raw、remark-gfm 从 package.json 移除(全仓已无引用)。
- plugins 对象提为模块级常量 STREAMDOWN_PLUGINS:Streamdown 的 memo 按引用比较 plugins,
  原内联字面量让流式渲染每个 token delta 都重建整棵 Markdown 树(顺带修复的既有性能缺陷)。
- citation 适配(渲染验证捕获的真回归):管线末端 rehype-harden 只放行可解析 URL,citation
  的裸相对 href(如 "42")会被整体替换成 [blocked] 占位节点。preProcess 统一把 citation 链接
  改写为 fragment 形态 [citation,domain](#id)(harden 显式放行 fragment;(?!#) 保证幂等),
  覆盖自生成与 LLM 按搜索提示词直接输出在正文里的两种来源;a 组件剥 # 还原内部 id。
- CSP 纵深防御落在**服务端响应头**而非 tauri.conf.json:壳窗口加载的是 http://127.0.0.1 页面,
  tauri.conf 的 csp 只对 tauri://asset 协议生效,对 remote URL 是死配置(保持 null 不动)。
  static.ts 给所有 HTML 响应下发最小 CSP:object-src none / base-uri self / form-action self /
  frame-ancestors self,同时覆盖 Web 托管与反代模式。script-src 经论证**不能**收紧:
  ① 构建产物 index.html 含 Vite 内联 <script>;② workbench 的 HTML 预览 iframe
  (sandbox=allow-scripts + srcDoc)按设计执行模型生成的任意内联脚本,而 srcdoc 文档继承
  父页 CSP——收紧即杀特性。脚本注入的主防线由 rehype-sanitize 承担。
- 行为变化(有意):正文原始 HTML 现按 defaultSchema 白名单过滤(script/iframe/onerror 等
  剥除,sub/sup/br 等安全子集保留);javascript:/data: 链接协议被消毒;裸相对链接(如
  [x](example.com),缺协议)会显示 [blocked] 占位——此类链接旧管线下也是坏链,可接受。
- 回归:markdown-sanitize.test.tsx 用 react-dom/server 渲染真实组件断言产物 HTML:
  XSS 五件套(img onerror/iframe/script/javascript: 链接/安全子集放行)+ 能力六件套
  (行内/多行块级公式、(..) 与 [..] 预处理、citation 徽章、GFM 表格、行内代码+普通链接)。
  tsconfig exclude 补 **/*.test.tsx 对齐既有测试排除策略(bun test 负责执行,tsc 不检)。

### R5-1(P2)shutdown 反代穿透 → 已修
- server.ts:带 X-Forwarded-For / X-Real-IP / Forwarded 任一头即 403——Tauri 壳直连本端口
  绝不经代理,经代理的请求一定不是壳发的;回环判定继续拦远程直连。不动壳侧契约。
- 回归:shutdown-endpoint.test.ts 新用例:三种转发头各 403 且服务不退,随后无头直连正常停机。

### R1-3(P2)静态路径前缀校验无分隔符 → 已修(+同模式补获一处)
- static.ts:startsWith(staticRoot) → startsWith(staticRoot + sep)。经 WHATWG URL 规范化,
  该向量实际不可达(.. 与  均被 URL 解析归一),属纯纵深防御,与审查判断一致。
- 同模式补获:tools/skills.ts:130 裸 startsWith(resolve(dir)) **真实可利用**——上游只拦了
  ".." 与 "/" 开头,Windows 盘符绝对路径(C:/.../<dir>X/...)可写进同前缀兄弟目录;按该文件
  既有双分隔符惯例补齐。files/index.ts、skills-import.ts 复查已带分隔符,无需动。

### R5-6(P3)ASR WS 裸 JSON.parse + keybindings in 检查 → 已修
- server.ts ws message:JSON.parse 包 try/catch,非法帧静默丢弃(合法客户端只发 start/stop)。
- settings.ts keybindings:in → typeof string + Object.hasOwn,原型链键(__proto__/
  constructor/toString)与非字符串 action 一律 400。
- 回归:asr-ws-frame.test.ts(真服务器发垃圾帧,断言连接不断、API 存活);
  keybindings-guard.test.ts(原型链键×4 + 非字符串 + 未知 action 全部 400;成功路径涉真实
  持久化,由应用层与既有 e2e 覆盖,单测只锁拒绝语义)。

### R5-5(P3)update 信任面 → 已修(①③,含超出记录的加重项)
- ①主机白名单:pub-[a-f0-9]+.r2.dev 泛匹配(任何人的 R2 公共桶)收紧为 UPDATE_R2_BASE 的
  精确 host(new URL 派生,改基址自动跟随);github.com 精确匹配 + *.githubusercontent.com
  锚定子域格式。
- ③下载流补 ReadableStream cancel 回调:cancel → abort 上游 fetch → 读循环抛 AbortError →
  finally 统一收尾(writer.end() 关句柄)。**加重项**:半截文件必须删——复查发现
  probeCachedInstaller 把"文件名含版本号且 size>0 的 .exe"当作可直接安装的缓存安装器,
  残留半截安装包会在下次检查更新时被当成完整品端给用户;现在 !downloadComplete 即 unlink。
  send() 包裹 enqueue 防断开后写已关流。
- ②签名/哈希校验维持审查结论:接受现状(HTTPS+白名单),release 流程产 sha256 后再接。

### R8-3(P3)助手正则重编译+ReDoS → 已修
- assistant-regex.ts 三层防线:①模块级编译缓存(同 pattern 只编译一次,null=编译失败或已判
  病态,会话内禁用;300 条容量护栏防恶意导入撑爆);②pattern 长度上限 500;③超时降级:单次
  replace 超 50ms 即拉黑该 pattern 并 console.warn。JS 无法中断执行中的正则,但流式渲染文本
  逐段增长、回溯成本随长度指数上升,会在几十 ms 量级被截获,而非跑到秒/分钟级。
- 残余风险(已记录在代码注释):一次性渲染超长历史文本时首跑可能超预算较多;彻底根除需 RE2
  类线性 wasm 引擎,对 P3 威胁面不值得引入。
- 回归:assistant-regex.test.ts 新增 4 用例:非法正则透传、超长 pattern 拒绝、缓存复用的
  lastIndex 正确性、灾难性回溯 (a+)+$ 首跑后被拉黑且复跑立即返回。

### 验证汇总(批次二)
- pc-server:tsc 干净;bun test 213/213(35 文件)。
- web-ui:tsc 干净;bun test 39/39(7 文件);bun run build 生产构建通过。
- XSS payload 集回归 = markdown-sanitize.test.tsx;反代演练 = shutdown 新用例;
  恶意帧/键 = asr-ws-frame + keybindings-guard。
## 批次三修复记录(2026-07-27)

批次主题:挂死与看门狗——无人值守可用性。八条发现全部按根源性方案落地,验证:pc-server 219 测试 + web-ui 39 测试全绿,双端 tsc 干净,前端生产构建通过。

### R3-1/R3-5 主链路超时看门狗:下沉为 tool-loop 骨架能力(三家统一)

- 根源:OpenAI 独有两层超时(头超时 180s/600s + 读流 120s 空闲),Claude/Google 裸奔——上游黑洞时永久悬挂,generating 卡死不可自愈。
- 方案(兑现 tool-loop.ts 原注释预告的"行为修复单独提交"):
  - **头超时+signal 桥接下沉骨架**:ProviderRoundAdapter 新增 `headerTimeoutMs(requestBody)`,`fetchRound` 改为接收骨架桥接后的 signal;骨架内 `fetchRoundWithHeaderTimeout` 统一建 controller、桥接外部 signal、response settle 即 cleanup(逐字搬迁自 OpenAI 原包装,OpenAI adapter 的自建包装随之删除)。Claude/Google 流式 600s,OpenAI 保持非流式 180s/流式 600s。
  - **空闲看门狗统一 helper**:tool-loop.ts 导出 `readWithIdleTimeout` + `STREAM_IDLE_TIMEOUT_MS`(120s),readClaudeStreamingRound/readGoogleStreamingRound/readOpenAiStream 三家读循环统一包装。原 readOpenAiStream 内联版删除,连带修复 R3-5(报错文案写死"10min"与实际 120s 自相矛盾、注释指向早已拆分的 server.ts:6063)。
- 同模式核查:其余裸 reader.read()(备份/导入/更新下载=进度流总时长上限无意义、aux 流/TTS/checks=fetchWithTimeout 的 AbortSignal.timeout 覆盖响应体读取全程)均已有界或属既有设计豁免;checks.ts 的 120s race 语义正确(有 clearTimeout,reject 即目标行为)保留。

### R3-4 停止后本轮工具不再执行(三家统一,含非流式路径)

- 根源:Claude/Google 只在轮首查 abort,"流读完→工具执行前"窗口不查;tool-loop 工具执行循环内部也不查——用户点停止后有副作用的工具(MCP 写/剪贴板/TTS)照样整批执行。
- 方案:runStreamingToolLoop 读流后无条件查 abort(原为 OpenAI 独有开关),工具执行 for 循环每迭代前查 abort 抛 AbortError(半消息落库路径已有);删除 abortCheckAfterRead 冻结点位(接口+三家 adapter+测试)。非流式路径 fetchClaudeTextWithTools/fetchOpenAiText 同样补轮首+每工具前检查。
- 测试:tool-loop.test.ts 新增两条——读流后中止抛 AbortError(不再依赖开关)、一批两工具第一个执行后 abort 则第二个被拦下。

### R3-2 MCP SSE 握手预算修正(1s → 真 15s)

- 根源:每次 reader.read() 被 Promise.race 包 1000ms reject 且无捕获,服务器静默超 1 秒整个握手即抛 timeout,外层 15s 预算永远达不到;setTimeout 从不清理。
- 方案:重写 readMcpSseUntilEndpoint——1s 定时器改为 resolve 哨兵的"tick 唤醒",醒来只复查外层 deadline 后 continue;关键细节:**复用同一个 pending read promise**(Web Streams 不允许同 reader 并发 read(),tick 后重新 read() 会抛 already reading),read 消费后才置空;每轮 finally clearTimeout。
- 测试:mcp-sse-handshake.test.ts(导出该 helper)——静默 1.3s 后 endpoint 到达仍成功(旧实现 1s 必死)、总预算耗尽才失败且报错含预算秒数。

### R3-3 ssePostEndpoint 迁出 settings(运行时缓存不落盘)

- 根源:SSE 型 MCP 的 POST endpoint(常带 sessionId)首次握手后直接写在 settings.mcpServers 元素上并持久化;服务器重启后旧 endpoint 失效,重试只换 session 不换 endpoint,重启 PC 也救不回来。
- 方案(三处):①mcp.ts 新增内存 `mcpSsePostEndpointCache`(与 mcpSessionCache 同键同生命周期),握手结果只进内存;②mcpJsonRpc 失败重试同时清 session+endpoint 两缓存再重新握手(任一缓存命中即值得重握手),mcpSessionHeaders 全部 initialize 失败时也清 endpoint 防卡死旧值;③settings.ts 移除该字段的持久化写入并剥掉前端经 ...body 回传的旧值,normalizeState 每次加载无条件剥除磁盘旧值(幂等、无需迁移标记、天然覆盖备份恢复)。
- 测试:mcp-endpoint-strip.test.ts——持久化旧值被剥除且其余字段原样、无该字段的服务器原样通过。

### R3-10 MCP tools/call 超时放宽(30s → 300s)

- postMcpJsonRpc 增加 options.timeoutMs(默认 30s 不变),mcpJsonRpc 按方法区分:tools/call 300s(对齐本地生图),initialize/list 等控制面仍 30s(本应秒级返回);超时报错文案随实际预算显示。

### R6-1 前端 SSE 客户端活性看门狗(45s 无字节即重连)

- 根源:services/api.ts sse() 与 SharedWorker(app-events-worker)两层读循环都不消费服务端 15s 心跳,半开 TCP(合盖睡眠/换网/VPN 断开)时 reader.read() 永久挂起——不抛错不重连,事件通道静默死亡直到手动刷新。
- 方案(纯客户端,45s=3×服务端心跳):两处均改为每连接独立 AbortController(外部 stop 桥接进来),收到任何字节(含心跳注释帧)即重置 45s 定时器,超时 abort 本次连接;sse() 用 idleTimedOut 标志区分"看门狗 abort→retry 走既有退避重连"与"外部 abort→fatal 终止订阅"(二者都表现为 AbortError,判定顺序关键);finally 统一清定时器+摘监听。会话详情流同走 sse(),一并受益。

### R7-4 长耗时操作可取消(压缩/标题;翻译澄清)

- 现状澄清:翻译端点早已改为立即返回 202、后端异步流式+SSE 推送,前端 `timeout: false` 是无意义残留,移除(回归默认 30s,实际毫秒级返回)。
- 压缩(真正的长同步请求):前端接 AbortController(`{ timeout: false, signal }`),压缩框取消键不再 disabled——压缩中点击=中止请求并关框,用户取消不弹错误 toast;**后端硬保证**:compressConversation 接收 request.signal,分块间查取消(不烧后续 LLM 轮次)+ 落库前最后一道闸(压缩是破坏性替换,取消后 LLM 结果作废绝不改写会话),失败/取消统一清掉"正在压缩..."瞬态标签并广播(顺带修复 LLM 失败时标签残留的原有缺陷)。
- 标题重生成:前端设 120s 上限(ky 超时会 abort 底层请求),后端拿到标题后先查 request.signal.aborted,取消/超时断开则返回 499 不改写标题。

### 冗余清理

- 删除:abortCheckAfterRead 接口字段与三家赋值、OpenAI adapter 自建 fetchRound 超时包装、readOpenAiStream 内联 idle-timeout(含过期注释)、settings 的 ssePostEndpoint 持久化写入、translate 的无意义 timeout:false。tool-loop.ts 头部"行为冻结纪律"注释更新为已兑现状态。临时补丁脚本用后即删。

### 验证摘要

- pc-server:tsc 干净;219 测试全绿(新增 6:tool-loop R3-4×2、MCP 握手×2、endpoint 剥离×2)。
- web-ui:tsc 干净;39 测试全绿;生产构建通过。
- 待人工验证(需真实环境):合盖睡眠/断网恢复后事件通道 45s 内自动重连;慢冷启动 SSE MCP(>1s)可连接;压缩中途取消后会话未被改写。

## 批次四修复记录(2026-07-27)

批次四"升级与启动韧性"三项全部完成。pc-server 232 测试全绿(新增 13:启动韧性 7、
数据目录卫生 6)、web-ui 39 全绿、双端 tsc 干净、前端生产构建通过、cargo check 干净;
8000 条旧版会话的端到端迁移演练通过(迁移期业务 API 全程 503、进度端点实时回报、
就绪后活库完整、state.json 瘦身)。

### R1-1 壳 20s 超时 × 首启同步迁移链(P1)——审查建议 ①+②+③ 全做

- 服务端"先绑端口再迁移":bootstrap(实例锁除外)从 Bun.serve 之前移到端口标记打出
  之后异步执行。实例锁仍前置(锁冲突必须抢在壳拿到端口之前退出,否则壳导航到死窗口);
  选端口所需 preferredPort 改由 peekPreferredPort() 对 state.json 轻量窥探(任何异常
  回落 8080,损坏场景 loadState 的恢复链稍后自会接管)。
- 迁移期闸门:新增 foundation/startup-gate.ts(ready/failed/phase/current/total 单一
  事实源)。fetch 入口在 Origin 检查后:/api/startup/status 始终可达且免鉴权(state 未
  装载时鉴权本就无法评估,端点不含机密);未就绪时其余 /api 一律 503(shutdown 除外),
  静态资源照常。flushAllStateBeforeExit 增加未就绪守卫:state 未装载时只释放实例锁,
  绝不对未初始化 state 调 saveState(迁移链自身崩溃安全:完成标记后置+逐会话幂等)。
- 迁移让出事件循环(否则 503/进度端点在同步迁移期间根本无法被服务):loadState 全链
  异步化;灌库改 migrateConversationsIntoDbBatched(200 条/事务,批间 yield+进度回调,
  复用原单事务函数、幂等重灌);file-dedup 的 sha256 逐文件 yield+进度回填。
- 前端迁移进度页:api.ts 收到 503 先探 /api/startup/status,确属未就绪才广播事件;
  新增 StartupGate 组件(仿 WebAuthGate 整页遮罩):阶段文案+计数每秒轮询,就绪自动
  reload;引导失败(failed)时展示原因——服务端引导失败改为不退进程、保持 503+failed
  状态,release 壳下这是用户唯一能看到真实原因的地方。文案 zh-CN/en-US 双语。
- 壳(lib.rs):20s 硬超时删除,改为"子进程存活即继续等"(每 10s 记一行日志);就绪
  等待移出 setup 主线程(阻塞主线程会冻结窗口)。真正的失败全部由 child_dead 分支兜住。

### R1-4 壳启动失败诊断误导 + 死契约(P2)

- 服务端所有致命退出点统一打单行 stdout 标记 `RIKKAHUB_FATAL:<code>:<message>`
  (code 对齐退出码:1 未知/绑定失败、2 端口耗尽、3 实例锁;消息为用户可读中文)。
  从未被壳解析的 `port_in_use:` stderr 死契约删除。
- 壳 stdout 泵捕获 FATAL 标记;child_dead 时优先弹标记带出的真实原因,无标记才回
  通用文案——不再无条件断言"端口被占用"(双开/实例锁场景曾诱导用户反复改端口)。
- 顺手修 8080 死页:窗口初始加载早于绑定会落在连接失败页,旧导航逻辑只在"非 8080"
  时用 location.href 猜测重导航。现 root.tsx <head> 内联 __RIKKAHUB_APP__ 旗标,壳在
  端口标记到达后统一按"旗标在且端口对才不打扰"重导航(3 次×700ms),任何端口的
  死页都能救活。无引用的 SIDECAR_PORT 常量一并删除。

### R1-13 数据目录卫生(P2,交互不可用,按推荐项决策并如实记录)

- 现状核实:recovery 类三条(成功落盘即清/.applied 超龄 30 天/.tmp 年龄门)批次一已
  覆盖;确认删会话不删物理附件(孤儿无限累积)、pre-memory-split.bak 全代码无读取方、
  pre-sqlite.bak 仍是活库损坏恢复链最后兜底。
- 新增 persistence/data-dir-hygiene.ts,就绪后台执行(自捕获,失败只留痕):
  ① corrupt-* 隔离(活库+memory/):同族保最新一份(唯一取证副本,无论多老),其余
  超龄 30 天清;同 mtime 按文件名时间戳破平。② updates/:文件名可解析版本且 ≤ 当前
  APP_VERSION 的安装包与 extracted-* 即删(纯可再生),解析不出版本一律保留。
  ③ pre-memory-split.bak:迁移标记已写 + 超龄 30 天双条件退役;pre-sqlite.bak 原样
  保留(退役需先补替代兜底,未做)。④ 孤儿附件:只统计不删(复用导出侧
  collectReferencedFileIds,每 10 次启动一次,错误中心 info 提示条目数/体积与人工
  处理建议)——自动 GC 需与安卓删除语义对齐,留后续专项。⑤ 备份类收敛 backups/
  子目录:缓做(要改恢复链全部路径,收益仅目录可读性)。所有清理错误中心 info 留痕。

### 冗余清理

- 删除:壳 SIDECAR_READY_TIMEOUT 与 SIDECAR_PORT 常量、旧"仅非 8080"导航块及其
  href 猜测守卫、server.ts 的 port_in_use 死契约行、bootstrap 内的实例锁段(移交
  server.ts 单点)。peekPreferredPort/卫生清扫均为 In 参数化变体+薄包装,无重复实现。
  临时补丁/演练脚本用后即删。

### 验证摘要(批次四)

- pc-server:tsc 干净;232 测试全绿(新增 13:peekPreferredPort×3、startup-gate×2、
  分批灌库×2、卫生清扫×6)。web-ui:tsc 干净;39 全绿;生产构建通过。壳:cargo check 干净。
- 端到端演练:8000 条旧版会话首启迁移——端口标记先于迁移打出;未就绪窗口业务 API
  503 命中 6/6;进度端点回报 migrate-conversations 7200/8000;就绪 434ms;活库 8000 条、
  state.json 瘦身、四个迁移标记齐全。双开演练:第二实例打出 RIKKAHUB_FATAL:3 退出码 3。
- 待人工验证(需真实环境):Tauri 壳真机走一遍"老版本大数据升级"(迁移进度页
  可见、壳不再 20s 杀进程);双开壳弹"实例锁"而非"端口被占用"文案;休眠唤醒后
  __RIKKAHUB_APP__ 导航守卫不误伤已加载页面。

## 批次五修复记录(2026-07-27)

**范围**:阶段九批次五全部 6 条——R8-2(P2)+ R6-3/R6-4/R6-2/R7-2/R7-3(P3×5)。

### R8-2 "保存窗口内键击丢失"竞态:三件套抽成共享 hook,15 处自动保存统一换装

- 病根回顾:各分区自动保存都是"dirtyRef + 防抖 effect"样板,保存完成回调无条件
  `dirtyRef=false`——键击落在"保存发起→resolve"窗口内时 dirty 标记被抹掉,下一轮防抖
  跳过,字符显示在框里、提示"已自动保存",实际永不落盘,切页即丢。extensions.tsx 的
  McpServerEditor 曾就地修过一次(savingRef 三件套),其余分区未同步。
- 终极修复:新建 `web-ui/app/hooks/use-autosave-draft.ts`,把三件套抽成唯一实现:
  - `createAutosaveController`(纯逻辑工厂,可注入假定时器测试)+ `useAutosaveDraft`
    (React 包装,save/onSaveError 每渲染取最新闭包)。
  - 语义:markDirty 置脏并重排防抖;保存完成 `dirty = 窗口内是否有编辑`,有则立即补排
    下一轮(键击不丢);失败保持脏、不自动重排(防持续失败风暴),下次编辑重试;
    saveNow 与在飞保存串行化(绝不并发双写);**卸载时若仍有脏数据尽力补发一次**——
    原样板卸载只清定时器,700ms 内的最后编辑直接丢,这是"切页即丢"的另一半病根。
- 换装清单(15 处,均删掉本地 dirtyRef/防抖 effect/重复 save 样板;speech 两面板为事后补获,见下):
  - `providers.tsx`、`assistants.tsx`、`search.tsx`、`proxy.tsx`(代理+端口两套)、
    `data.tsx`(WebDAV+S3 两套)——审查点名的 5 分区;
  - 同模式检索补获:`general.tsx` 昵称输入(同病:profileDirtyRef + 无条件清脏 +
    重对齐 effect 无脏守卫);`extensions.tsx` 除 McpServerEditor 外还有 Lorebook/
    QuickMessage/PromptItemEditor(模式注入)/Skill 四个编辑器带无三件套的裸样板,一并换装;
    McpServerEditor 本体也换到共享 hook(patchCommon 即时保存走 draftRef 同步写 + saveNow)。
- 连带修复的"双倍病"(重对齐 effect 把保存窗口内键击当场可见地清掉):
  - `assistants.tsx`:重对齐 effect 原依赖 `settings.assistants`,每次 autosave→onSettings
    回环重触发。改 assistantsRef + 仅依赖 assistantId(同 providers/extensions 既有模式)。
  - `data.tsx` 两套、`general.tsx`、`proxy.tsx` 两套:重对齐/回填 effect 补 `isDirty()` 守卫。
  - `extensions.tsx` PromptItemEditor:脏复位 effect 原依赖 `[selectedId, items]`,items 每次
    回环变化即清脏;改仅 selectedId。Skill 编辑器:每次 autosave→load() 换新 selectedSkill
    触发内容回填 effect,补 isDirty 守卫不再冲掉在飞键击。
- 顺带冗余清理:search.tsx 手动 save 与自动保存本是两份重复的 POST+合并逻辑,统一为
  hook persist 体;其"测试前保存顺带改写全局 searchServiceSelected"属自动保存之前的遗留
  (测试一个服务不应劫持全局选中),一并去除。proxy/data/general/extensions 各删一段
  announce=true 手动保存死路径(无调用方)。删除 11 个随之死掉的翻译词条
  (proxy.saved/port_saved、search.saved、webdav_saved/s3_saved、mcp.server.saved、
  mcp.lorebook.saved、mcp.quick.saved、mcp.item_saved、mcp.skill_saved、general.profile_saved,
  zh-CN + en-US 双语言包)。
- 回归测试:`use-autosave-draft.test.ts` 9 项(核心竞态:保存窗口内编辑保持脏并自动补排、
  第二轮拿到最新草稿;失败不自动重排;saveNow 串行化;卸载补发等)。
- **事后补获(2026-07-27,用户追问"够不够终极"时自查发现)**:`speech.tsx` TTS/ASR 两面板
  是第 14/15 处漏网——此前检索模式匹配 `}, [settings.xxx]` 字面依赖,而它的重对齐 effect
  依赖的是派生变量 `providers`(= settings.ttsProviders),没扫到。病是双倍的:patchDraft
  每次编辑 setTimeout(0) 整包立即保存(每键一个 POST),保存回环 onSettings → providers
  新数组 → 重对齐 effect 重触发 setDraft,把在飞键击当场冲掉。修复:两面板换装共享 hook,
  重对齐改 providersRef + 仅依赖 selectedId + reset();TTS 测试按钮依赖"每键即存"才能用
  providerId 覆写取到最新配置,改防抖后测试前补 `autosave.saveNow()` 落盘。
  教训再 +1:同模式检索不能只匹配字面 `settings.` 依赖,派生中间变量会藏病。
- **同批增强:reset() 改为先补发再清态**——原语义"切换条目直接丢弃待保存状态",意味着
  "输入后 700ms 防抖窗口内点了另一个条目"会静默丢字。现 reset 在清态前对脏编辑尽力补发
  一次(调用约定:reset 与 setDraft(新实体) 同步于同一 effect/handler,React 尚未重渲染,
  save 闭包仍指旧实体,不会串写);已在飞的保存不重复发起。15 处调用方零改动受益。
  回归测试更新为 3 项(补发、干净不发、在飞不并发),web-ui 共 54 项全绿。
  仍存的理论窗口:"编辑+切换同落在一个保存 RTT 内"时该键击不可恢复(hook 无法回溯
  旧草稿快照),概率极低,已注释说明。

### R6-3 密码闸门:弹窗已开时重复 401 清空正在输入的密码

- `web-auth-gate.tsx`:onWebAuthRequired 处理器加 openRef 守卫——弹窗已开时后台请求再触发
  的 401 只保持弹窗,不再重置 password/error/submitting。

### R6-4 unhandledrejection 兜底:AbortError 过滤 + 本地 30s 风暴合并

- `root.tsx`:AbortError 名称的拒绝直接忽略(导航/卸载取消请求属正常流)。
- `app-errors-store.ts` reportLocalError 复用后端"同 domain+message 30s 合并计数"语义:
  窗口内只累加既有条目 count、返回 false,调用方跳过重复 toast;窗口锚定首条 at 不滑动,
  持续风暴每 30s 至多一条新条目 + 一次 toast。回归测试 4 项。

### R6-2 SharedWorker 异步加载失败回退直连

- `app-events.ts`:接 `worker.onerror`——脚本异步加载/求值失败(支持 SharedWorker 但不支持
  module worker 的 WebView、脚本 404 等)时,未收到任何消息前一次性切 startDirect();
  已有消息后的运行期错误不回退(通道已建立,再开直连会双份分发),迟到的 worker 消息丢弃。

### R7-2 模型错误横幅:正文关键词正则 → 服务端结构化标记

- 服务端:`foundation/types/dto.ts` 注解联合新增 `ModelCallErrorAnnotation`
  (`{type:"model_call_error", message}`);`orchestrator.ts` catch 落盘失败文本的同时把标记
  push 进 `currentMessage.annotations`(与消息一同持久化/出线,DTO 零拷贝直传);重入生成
  (续写/重试复用同一消息对象)在起点清除旧标记,成功后横幅不残留。
- 前端:`chat-message.tsx` 的 `messageHasModelCallError` 改查注解,删除
  `/请求失败|request failed|timeout|401|.../i` 正文正则——正常讨论 HTTP 状态码/超时/报错文案
  的回答不再误挂"打开模型设置"横幅。`chat-message-annotations.tsx` 的引用过滤补类型谓词
  (联合扩展后仍只渲染 url_citation)。
- 注:历史失败消息(标记落地前产生)不再显示横幅——按"结构化信号驱动"的目标语义,
  正则误报面清零优先于旧数据横幅保留。

### R7-3 轮询单调守卫:序列化长度代理 → updateAt 单调量

- `conversations.tsx` 10s 兜底轮询:原"节点数相同则比最末节点 JSON.stringify(parts).length,
  更短即弃"会把合法的改短编辑(改短文本/切换更短候选分支)误判为旧数据。改为
  `data.updateAt < prev.updateAt` 即弃:服务端每次内容变更/流式 chunk 都置
  `updateAt=Date.now()`(touchStream/各 handler),且 node_update 帧携带 updateAt 并由
  applyNodeUpdate 同步进客户端 detail,天然单调、等值幂等接受。

### 验证

- pc-server 232 测试全绿;web-ui 52 测试全绿(新增 autosave 9 + 错误合并 4);
  双端 tsc 干净;web-ui 生产构建通过。Rust 壳无改动。
- 待人工验证(需真实环境):高延迟(反代/节流)下设置各分区连续输入不丢字符、
  五分区自动保存回归;模型调用失败后消息下方出现"打开模型设置"横幅、重试成功后消失;
  讨论"500 错误怎么排查"类内容不再误挂横幅。

## 批次五专项复审记录(2026-07-27,应用户要求二次全面审查)

**方法**:把批次五动过的每一处代码当作嫌疑代码重新逐行审——重点查修复本身引入的新竞态、
接线错漏、边界未兜。结论:R6-2/R6-3/R6-4 与 R7-2/R7-3 链路验证干净(R7-3 专门核了流式
逐 chunk 推 updateAt——touchStream 在 sink 模式的 streamHooks 不带 sink 字段故全量执行,
node_update 帧携 updateAt 且前端 applyNodeUpdate 同步进 prev;变更端点 15 处 updateAt
配对齐全)。R8-2 一侧查出 5 项真问题,已全部修复:

### F1(最严重,数据正确性):删除复活——autosave 缺"丢弃"语义

- 病根:全部 10 处删除流程都是 `api.delete → 更新列表 → setSelectedId(下一条)`,随后
  重对齐 effect 调 `autosave.reset()`。若删除前 700ms 内用户编辑过(草稿仍脏):
  ① reset 的补发把**已删实体 POST 回 detail 端点,服务端复活**(speech/search 的 persist
  还会把它重新插回本地列表);② 即使没有 reset 补发,防抖定时器也可能在 `api.delete` 与
  重对齐之间的异步间隙触发,同样复活;③ 在飞保存的 POST 可能后到于 DELETE,服务端乱序复活。
  ②③在换装前的 dirtyRef 样板里就存在,不是批次五新引入,但一并根治。
- 终极修复:hook 增加 `discard(): Promise<void>`——丢弃待保存状态且**绝不补发**,并等待
  在飞保存收尾(保证 DELETE 不与迟到 POST 乱序)。与 reset 的语义分工:reset 面向"切换
  实体"(脏编辑要补发),discard 面向"实体即将消失"(脏编辑必须丢弃)。
- 接线:10 处删除流程(providers/assistants/search/speech TTS+ASR/extensions 的
  mcp-server、lorebook、quick-message、prompt-item、skill)全部在 `api.delete` 前
  `await autosave.discard()`。

### F2(批次五回归):删除后不重对齐,草稿停留在已删实体上

- 病根:批次五把重对齐 effect 依赖收窄到 `[selectedId]`(治"保存回环冲键击"是对的),
  但 lorebook/quick-message/prompt-item 的删除流程**不改 selectedId**,mcp-server 的删除
  把 `setSelectedId("")` 放在 `pullSettings` 之前(effect 用旧列表兜底到 servers[0]——
  可能正是刚删的那条)。后果:删除后表单仍显示已删实体,用户再编辑一笔就经自动保存复活它
  (即使有 F1 的 discard 也挡不住这条"删除后再编辑"路径)。
- 修复:四处删除流程改为"先 `pullSettings` 再显式 `setSelectedId(下一条)`";删到空时
  (重对齐 effect 无条目可载、EditorShell 仍渲染表单)复位为挂载空列表时同款的空白新草稿
  (createX()/createSearchService()),search.tsx 同病同修。providers/assistants/speech
  的删除本就显式选中下一条且空态有渲染守卫,仅补 discard。

### F3(漏保存):providers 类型切换直接 setDraft 不置脏

- `providers.tsx` 供应商类型 Select 的 onValueChange 直接
  `setDraft(normalizeKindPatch(...))`,不 markDirty——切换类型永不自动保存,直到用户
  恰好再改别的字段。补 `autosave.markDirty()`。

### F4(hook 边界):reset 补发失败把脏标记留给新实体

- reset 的补发若失败,runSave 的 catch 无条件 `dirty = true`——此时草稿已换新实体,
  调用方的 isDirty 守卫会一直挡掉外部值回填(SSE 推送不生效)直到下次保存成功。
  修复:补发失败时 `dirty = editedDuringSave`(失败窗口内新实体已有编辑才保持脏,
  其定时器自行重试;否则不背旧实体的锅)。

### F5(健壮性,数据安全):R7-2 清标记对 annotations 缺失不容忍

- `orchestrator.ts` 生成入口 `currentMessage.annotations.filter(...)` 若遇到 annotations
  字段缺失的异常数据(手改/极老数据;工厂与 backup/import 均会初始化,概率极低)会在入口
  抛 TypeError,**该会话生成永久性砸死**。改 `(currentMessage.annotations ?? []).filter`
  顺手自愈为数组。

### 验证

- hook 新增 3 项回归测试(discard 丢弃不补发、discard 等待在飞收尾不乱序、reset 补发失败
  不留脏),web-ui 57 项全绿;pc-server 232 项全绿;双端 tsc 干净;前端生产构建通过。
- 手工验证补充项:编辑供应商任意字段后 1 秒内点删除并确认,确认服务端不复活该供应商;
  lorebook/快捷消息/模式注入删除后表单立即切到下一条(或空白新草稿);切换供应商类型后
  不再做其它编辑,等 1 秒刷新页面确认类型已保存。
---

## 批次六修复记录(2026-07-27)

**范围**:阶段九批次六全部 6 条——R2-2/R2-3(P2×2)+ R2-4/R2-5+R1-6/R2-6/R3-7(P3×4)。
会话域竞态与 IO 卫生,全部服务端(pc-server),前端零改动。

### R2-2(P2):fire-and-forget 续体自持 checkout/release

- 病根:messages POST / messages edit / translate 三处 `void (async ...)` 续体在路由级
  try/finally release **之后**继续 await(OCR/翻译可长于 60s sweep 闲置期)再写会话。
  实例被 sweep 清出后续体写的是孤儿对象,generateAnswer 的 checkout 从活库装出第二实例
  ——流式写进孤儿、persist 的却是陈旧实例,屏上看得到回答重启后消失(UI 与磁盘分叉)。
- 修复(结构性):三个续体开头 `checkoutConversation`(IIFE 首个 await 前同步执行,与
  路由级引用无缝衔接,保证拿到同一实例)+ `finally releaseConversation`,覆盖续体全
  生命周期。generateAnswer 入口本就同步自持引用,续体无需 await 到生成结束。
- 同模式复核:`runPostGenerationTasks`(生成后标题/建议,orchestrator:607 fire-and-forget)
  **无需修复**——它从不跨 await 缓存实例,每次 await 后都经 getConversation(checkout+
  release,注册表优先)重新解析 live 实例,写与 persist 之间无 await,天然满足不变式。
  regenerate/tool-approval 的 `void generateAnswer` 在路由同步段调用,入口同步 checkout
  在路由 release 之前完成,亦安全。

### R2-3(P2):generateAnswer 入口"先 abort 旧流"守卫下沉

- 病根:端点级守卫(send/edit/regenerate 先 abort)只能中止已登记的生成;OCR 窗口内
  连发两条消息时,两续体先后异步触发 generateAnswer,后者直接 generating.set 顶掉前者
  controller——前者成无主流,两路流式交错写同一节点(parts 交叉污染)且无人能停。
- 修复:generateAnswer 入口先 `generating.get(id)?.abort()` 再登记新 controller,把
  端点级纪律下沉为编排器不变式(同会话任意时刻至多一路登记流,后到者胜)。
  completeConversationGeneration 按 controller 身份幂等(get(id) !== controller 即返回),
  旧流收尾不会误删新登记——守卫零额外状态。

### R2-4+R2-6(P3):删会话/清库的 SSE 侧收尾

- 新增 `api/sse.ts dropConversationSse(id)`:①逐 controller close 该会话已打开的详情流
  (此前只摘集合,连接悬挂只收心跳;close 后连接内建心跳在下一跳 enqueue 抛错自清,无
  定时器泄漏);②按 `convId::` 前缀清 pendingBroadcasts(流式中删会话时 orchestrator
  finally 提前 return 跳过唯一清理点,条目连同已删会话整树永久驻留,挂着的 33ms 定时器
  还会广播已删会话的陈旧帧)。deleteConversationsById 改调它。
- 新增 `clearAllPendingBroadcasts()`:导入清库(finalizeConversationImport)与
  clearWorkingSet 配对调用——待发广播挂着旧实例引用,定时器触发会把导入前的陈旧帧广播出去。

### R2-5+R1-6(P3):DB-first 遗留 saveState 清理 + addLog 节流

- 全仓 saveState() 调用点逐一分类核验:会话域 22 处全部无效——handlers/conversations.ts
  ×17、helpers.ts(deleteConversationsById)×1、orchestrator.ts×3(resumeApprovedToolParts/
  finalizeOutcome/生成入口)、auxiliary.ts(compressConversation)×1。state.json 已不含
  会话数据,这些调用每次全量 JSON.stringify 整个 state 并写盘,纯浪费 IO。全部删除,
  四个文件的 saveState 导入一并收敛。
- 核验依据:bumpAnalyticsMsgCount 是纯内存计数(analytics.ts 模块变量,不进 state),
  工具二进制落盘点 saveToolBinaryContent(改 state.files)自带 saveState——生成路径上
  唯一真正改 state 的位置自我负责,orchestrator 不需兜底。
- addLog(logs.ts):每条请求日志全量落盘改 `scheduleThrottledSaveState()`——日志/stats
  计数是可容忍秒级丢失的非关键数据,与流式期间既有节流语义一致。settings/files/memory/
  media/import 等域的 saveState 均真实改 state,保留。

### R3-7(P3):parseSseChunks 跨行 data 修复

- 病根:data 行 join 后又按行拆回,逐行 JSON.parse——跨多行的 data JSON(SSE 规范允许,
  某些中转站美化输出会触发)每行解析失败被调用方容错吞掉,内容整段静默丢失且无报错。
- 修复:对齐 readClaudeStreamingRound——join 后若整体是合法 JSON 则作为单一载荷返回;
  不是合法 JSON 再退回逐行拆分(兼容"同一 block 塞多个单行 JSON 事件"的不规范上游,
  也覆盖 CRLF 流因 \n\n 分帧不命中而多事件挤进一个 block 的场景)。单行载荷行为不变。
- 新增 parse-sse-chunks.test.ts 5 项回归(单行/[DONE]/跨行美化 JSON/多事件回退/CRLF)。

### 验证

- pc-server 237 项全绿(232+5 新增);web-ui 57 项全绿;pc-server tsc 干净。
- 手工验证补充项:开启 OCR 且用慢模型时连发两条带图消息,确认只有一路回答在流式且
  停止键有效;发消息后立即切走会话等 90 秒再回来,确认回答完整且重启后不丢;流式中
  删除该会话,确认详情 SSE 立即断开、无陈旧帧;高频工具调用会话观察 state.json 写盘
  频率明显下降。
---

## 批次六专项复审记录(2026-07-27,应用户要求二次全面审查)

**方法**:把批次六动过的每一处代码当作嫌疑代码重新逐行审——重点查修复本身引入的新竞态、
修复不彻底的残留路径、同类问题在相邻代码里的翻版。结果:R2-4/R2-6/R2-5/R1-6/R3-7 各链路
验证干净;R2-2/R2-3 侧查出并根治 4 项(G1 最重,属批次五 F1"迟到写复活已删实体"在会话域
的孪生,批次六之前就存在,非本批新引入,但一并根治)。

### G1(重,数据完整性):已删会话被"长 await 后落库"复活

- 病根:`persistConversation` 是无条件 upsert(重插会话行+整棵节点树);而
  `removeConversations` 删注册表条目**不看 refs**(删除意图优先,by design)。任何
  "长 await 后拿着旧实例落库"的代码,在 await 期间会话被删除时,都会把已删会话连整棵
  消息树重新插回活库——比批次五 F1(设置实体复活)更重:还会给僵尸会话触发
  generateAnswer 续写、广播,列表里"删不掉的会话"复现。
- 排查面:全部"长 await 后写会话"的路径,共 5 处中招——
  ① messages POST 的 OCR 续体(persist + generateAnswer);② messages edit 的 OCR 续体
  (同);③ translate 续体的 finally(persist);④ regenerate-title(标题生成 await 后
  persist);⑤ compressConversation(多轮 LLM await 后破坏性替换 + persist)。
  orchestrator 本体不中招:completeConversationGeneration 经 getConversation(已删返回
  undefined)跳过 persist,flushConvDirty 经 peekConversation 跳过,catch/finally 有
  conversationStillExists 闸;runPostGenerationTasks 每次 await 后重新解析 live 实例,
  天然安全。
- 修复(统一守卫):落库前 **身份比对** `getConversation(conversation.id) !== conversation`
  即丢弃结果(①②续体 return;③跳过 persist;④返回 404;⑤抛"会话已被删除,压缩结果
  作废"→ 400,与 R7-4 取消闸并列)。身份比对而非仅存在性:同时防"导入同 id 会话"后
  陈旧实例反向覆盖新数据。守卫与 persist 之间无 await,JS 单线程下原子。
- 回归测试:新增 api/continuation-delete-guard.test.ts(2 项)——mock 模块边界
  (attachOcrToImageParts=可控 deferred / persistConversation、generateAnswer=记录桩),
  真实路由驱动 messages POST:OCR 完成前删除 → 删除后零新 persist、零 generateAnswer;
  未删除路径 → 正常落库+触发生成(守卫不误伤)。

### G2(重,内容完整性):R2-3 接管后旧流收尾污染新流复用的同一消息

- 病根:入口守卫 abort 旧流 A 后,新流 B 经 ensureAssistantGenerationNode **复用 A 的空
  占位消息**(同一对象)。A 的 AbortError 异步浮出(可能已在 B 流式中),A 的
  finalizeOutcome 会对这条 B 正在写的消息做 applyOutputTransforms/finishReasoningParts/
  盖 finishedAt/写估算 usage;若 abort 以普通网络错误浮出还会走失败分支,把"请求失败"
  文本 + model_call_error 注解追加进 B 的回答。
- 修复(所有权判定):模块级 `activeGenerationMessages: Map<convId, Message>` 记录
  当前流正在写的消息;finalizeOutcome 开头判定——generating 登记是别的 controller
  (被接管)**且**所有权记录 === 本流的 currentMessage(新流复用了同一对象)时静默退出,
  消息由新流收尾;新流用新消息对象时旧流照常收尾自己的消息(行为不变)。
  completeConversationGeneration 幂等清理:登记被外部清除(stop/删会话)时顺手清所有权
  记录防陈旧引用滞留,被接管时不清(所有权归新流)。

### G3:abort 意图支配错误分类

- catch 分支由 `err.name === "AbortError"` 扩为 `controller.signal.aborted || AbortError`
  ——部分 fetch 实现/上游在 abort 时抛普通网络错误,原逻辑会给用户主动停止的回答追加
  "请求失败"并弹全局错误。中止意图一经发出,一律按中止收尾。

### G4:abort 收尾摘 loading 占位

- 中止发生在首个 delta 之前时,loading 占位没人摘(正常路径由首个 delta 摘、stop 端点
  手工摘,但删会话/接管/新消息 abort 不经过 stop 端点),前端"打字点"永久残留在已完结
  消息上。abort 收尾统一 filter 掉 loading part,与 stop 端点对齐。

### 复验干净项

- R2-4/R2-6:详情流对已删会话重连 → getConversation 404;openSse 心跳在服务端 close 后
  下一跳 enqueue 抛错自清,无定时器泄漏;abort 同步先于 dropConversationSse 且读器
  abort-after-read 检查(批3)保证之后无 touchStream,pendingBroadcasts 无"清后重添"窗口;
  Map 迭代中 delete 安全;导入路径 clearConvDirtyState+clearWorkingSet+整表清配对完整。
- R2-5:rg 核验 handlers/helpers/auxiliary/orchestrator 四文件零 state 写点,22 处删除
  结论性安全;flushConvDirty 先清标记再迭代,已删会话无脏标记滞留。
- R1-6:state.logs 本就不序列化落盘(内存态,重启清空),节流仅影响 stats 计数,
  硬杀窗口 ≤200ms,优雅关停有 flushAllStateBeforeExit 终态存盘兜底。
- R3-7:五个调用方逐一推演;CRLF 流(\n\n 分帧不命中致多事件挤一个 block)由
  "整体 parse 失败退回逐行"兜住,行为不劣于修复前。

### 验证

- pc-server 239 项全绿(237+2 新增);web-ui 57 项全绿;tsc 干净。
- 手工验证补充项:发一条带图消息后 1 秒内删除该会话,刷新+重启确认会话不复活;
  流式翻译中删除会话,确认不复活;OCR 窗口内连发两条消息,确认最终回答无"请求失败"
  残段、无重复内容、停止键有效;删除正在流式且尚未出字的会话,确认其它会话界面无
  残留"打字点"。

## 全域孪生病灶扫描记录(2026-07-27,应批5 F1/批6 G1 孪生问题的系统性排查)

病灶类定义:①跨**长 await**(LLM/网络/握手/防抖)持有实体引用 → ②await 后**无条件写回**
(upsert/全量快照 POST)→ ③目标实体在 await 期间**可能已被删除或替换** → 复活已删/陈旧反向覆盖。

### 逐域结论

- **会话域**:批6 G1 已修(身份比对守卫×5 + working-set 单实例保证)。
- **供应商测试**(markProviderTestResult):干净。按 id map + providerAuthChanged 比对,
  map 永不插入 → 已删则不命中,不可能复活(R5-3 同型防护,先前已建)。
- **搜索服务测试**(settings/search/service/test):干净。同上 map+鉴权字段比对模式。
- **MCP 域**:per-id 写锁(withMcpServerWriteLock)串行化同一服务器的 detail/sync/delete,
  互相交错的窗口已关;sync 在锁内现查现改。**但备份恢复/导入整体替换 settings 不走锁**,
  同步长 await 期间完成恢复时 upsertById 会把旧世界服务器插回新 settings → 见 H1/H2 修复。
- **memory 域**:干净。resolvePending 同步内核(查找→摘除→内存写→末尾统一落盘),
  无跨 await 捕获;CRUD 全为 id 现查 + 404 语义。
- **media/files 域**:干净。TTS/ASR/生图/上传均为返回结果或创建新条目(创建语义无复活问题)。
- **skills 域**:干净。导入为文件系统创建语义,写入的是新下载内容而非陈旧捕获状态;
  删除同步完成且清 enabledSkills。
- **update/data 域**:干净。更新下载写盘物无实体删除并发;WebDAV/S3 恢复是设计内整体替换。
- **prompt-optimize / provider 余额 / 生图测试**:只读或纯返回,不写 state。
- **前端**:批5 已收口 settings 域(15 处 autosave + 10 处删除 discard + speech.tsx);
  本轮复扫非 settings 域,chat-input/conversations 等定时器均为 UI 提示/滚动,无延迟实体写。
- **handler 外**:rg 核验 updateSettings/syncMcpServerTools 无 handler 之外的写点
  (checks.ts 的 markProviderTestResult 已验干净)。

### H1:MCP sync 导入穿透锁复活(api/handlers/settings.ts)

- settings/mcp-server/sync 在锁内 await syncMcpServerTools(长握手)后 upsertById 写回。
  per-id 锁挡不住备份恢复/导入(整体替换 settings)→ 旧世界服务器插回新 settings。
- 修复:写前身份重查(同批6 G1 守卫族)——起始捕获的条目引用已不在当前
  state.settings.mcpServers → 409 丢弃同步结果。身份比对成立的前提已核验:
  upsertById/deleteById/reorderByIds 均复用未触及项引用,updateSettings 直接赋值不克隆,
  同 id 写操作被锁串行化 → 引用失配当且仅当导入替换或该项被改写。

### H2:MCP detail 内嵌同步同型缝隙(api/handlers/settings.ts)

- settings/mcp-server/detail 保存启用中的服务器时内嵌一次工具同步(长 await),之后 upsert。
  同一缝隙:导入期间完成保存 → 陈旧写回。
- 修复:仅"更新既有服务器"(prevServer 非空)做身份重查,失配 → 409;
  新建(prevServer 为空)保持创建语义不受影响。

### 回归测试

- 新增 api/mcp-import-race-guard.test.ts(4 用例):真实路由驱动,mock syncMcpServerTools
  为可控 deferred,在长 await 期间 setState 模拟导入整体替换;断言 H1/H2 均 409 且服务器
  不复活;两条对照用例确认守卫不误伤正常保存与新建语义。updateSettings mock 为只改内存
  (json-store 不能 mock:state 是 let 重赋值绑定,展开会冻结快照)。

### 验证

- pc-server 243 项全绿(239+4 新增);web-ui 57 项全绿;tsc 干净。
- 冗余检查:本轮仅新增两处守卫与一个测试文件,无冗余代码产生。
- 手工验证补充项:MCP 工具同步进行中执行备份恢复,确认恢复完成后设置里不出现
  恢复前的旧 MCP 服务器条目。

## 批次七修复记录(2026-07-27,部署面与杂项 P3×6)

### R5-2:SSE 反代缓冲 + 反代文档

- 代码:api/request.ts 新增 sseHeaders() 单源构造(Content-Type/Cache-Control/Connection
  + X-Accel-Buffering: no),全部 text/event-stream 响应换装。审查记录 3 处,实扫 7 处:
  api/sse.ts(events/详情流)、settings provider/test/stream、update/download、
  **data.ts 的 WebDAV/S3 进度流×4(审查漏记,同病同修)**。出站请求的 Accept 头不动。
- 实测:启动服务器 curl /api/events,X-Accel-Buffering: no 在响应头中。
- 文档:README×3(EN/简/繁)Docker 段后新增"反向代理(nginx)"一节——Host 透传
  (proxy_set_header Host $host,否则 Origin/Host CSRF 校验把全部写请求 403)、
  WebSocket upgrade map、proxy_buffering off 兜底、read_timeout 1h、body size 0;
  并注明设 RIKKAHUB_PASSWORD。

### R3-9 注记:Docker SSRF(纯文档)

- README×3 Docker 段加安全注记:scrape_web 在容器内直接抓模型给的任意 URL,提示词注入
  可诱导探测内网/云元数据;在意则隔离容器网络或限制出站。不改代码(沿用"跟安卓一样"
  的既有决策)。

### R1-8:mime 表补齐

- api/request.ts mime() 补 woff2/woff/ttf/otf(字体上传端点接受的全部四种)+ json + wasm。
- 勾连核查:mime() 三个调用方(static.ts 静态资源、files.ts path 附件端点、icons.ts)
  均无 Content-Disposition 下载语义依赖,类型更精确无副作用。

### R1-7:系统代理读取异步化(stale-while-revalidate)

- 病灶:auto 模式下 fetch 拦截器 per-request 调 resolveEffectiveProxy,TTL(2s)过期后的
  首个出站 fetch 同步 spawnSync 两次 reg query,阻塞事件循环数十 ms,多路流式并发时
  表现为周期性微卡。
- 修复:reg/gsettings 读取全改 Bun.spawn 异步(runCommandText 单源,不留同步/异步两套);
  bootstrap 第 2 步 await primeSystemProxyCache() 预热(首个业务 fetch 前缓存必命中);
  readSystemProxy 变纯缓存读——TTL 过期返回陈值并触发后台单飞刷新,永不阻塞。
- 顺带:settings/proxy/detect 端点改用 detectSystemProxy(平台分发)——此前只探 Windows,
  Linux 桌面点"检测"永远返回空。
- 勾连核查:resolveEffectiveProxy/classifyProxyError/proxyStatusPayload 保持同步签名,
  拦截器与全部调用方零改动;唯一异步化消费方 detect 端点已 await。

### R1-11:提示词字数上限迁移去正则化

- 病灶:normalizeState 每次启动对 titlePrompt/suggestionPrompt 无条件正则替换
  "not exceed 10 characters"→15/18,用户有意写 10 的自定义提示词被反复静默改掉。
- 修复:无状态精确匹配——由现默认值(TITLE/SUGGESTION_CHARACTER_LIMIT 单源)重建
  "字数还是 10"的旧默认全文,整段完全相等才升级到新默认;任何自定义原样保留。
  不用迁移标记(pc-backup.json 不导出标记,恢复即丢——R1-12 教训),幂等、随备份天然往返。
- 新增 persistence/prompt-limit-migration.test.ts 3 用例(旧默认升级/自定义含 10 保留/
  空值回填+现默认幂等)。

### R4-6:备份 staging 文件名跨平台清洗

- 病灶:zip 内文件名直接用 file.fileName(仅防重名),Linux/安卓来源的 :*?"<>| 等在
  Windows 上让 copyFileSync 抛错,该附件永远缺席备份且用户无解。
- 修复:staging 名清洗非法字符(含控制符)为 _,剥结尾点/空格;清洗后为空回退 id 名。
  backupName 与原名本就解耦(manifest 与 zip 同源于同一产出),零副作用;清洗后重名由
  既有 usedNames 去重兜底。

### 批次七自查发现:e2e "只等端口标记"竞态(测试基建)

- R1-7 给 bootstrap 增加了代理预热耗时(reg query×2,约百 ms)后,5 个子进程 e2e 集体
  失败(503/WS 拒连)——暴露了测试侧潜在竞态:R1-1 之后端口标记先于就绪打出(先绑端口,
  迁移异步跑,期间 /api 一律 503),真实前端靠轮询 /api/startup/status 渲染进度页,
  而 5 个 e2e 文件各自复制的 waitForPortLine 只等端口就发业务请求,此前纯靠 bootstrap
  够快才碰巧通过。
- 根源修复:新增 test-utils/e2e-server.ts waitForServerReady(等端口 + 轮询就绪闸门,
  failed 即抛),5 个文件(android-passthrough/restore-markers-rollback/import-overwrite/
  shutdown-endpoint/asr-ws-frame)删除各自的重复 helper 统一换装(冗余清理)。

### 验证

- pc-server 246 项全绿(243+3 新增);web-ui 57 项全绿;tsc 干净。
- 实测:手动启动服务器正常(代理预热日志正确);curl 核验 SSE 响应头带 X-Accel-Buffering: no;
  R4-6 清洗正则对中文/非法字符/尾缀点空格/控制符样本行为正确。
- 手工验证项:nginx 反代全功能演练(Host 透传后登录/保存/流式/语音/备份上传全链路;
  对照去掉 proxy_set_header Host 应复现全 403)。

### 批次七自我复审补强(2026-07-27,应用户"是否足够根源终极"质询)

- 逐项按"根源性"标准重新拷问,五项经得起检验(单源构造/SWR 标准模式/无状态迁移/
  文档属决策而非补丁),查出两处需补强:
- ①R4-6 残余缺口:Windows 设备保留名(CON/PRN/AUX/NUL/COM1-9/LPT1-9,裸名或带任意
  扩展名)与超长文件名(>255 字节)仍会让 staging copyFileSync 炸。清洗逻辑抽成导出
  函数 sanitizeStagingFileName(export.ts),补③保留名加 _ 前缀、④超长截干保尾缀
  (150 字符),export-staging.test.ts 新增 3 用例(含 console.png/communication.md
  不误伤断言)。
- ②R5-2 缺防回归机制:单源构造挡不住未来新端点又手写头。新增 api/sse-headers-guard
  .test.ts 卫兵:扫描 api/ 全部非测试 .ts,除 request.ts 外出现手写 text/event-stream
  响应 Content-Type 即失败(出站 Accept 不受限)。
- 记录在案的既有权衡(决策,非缺口):反代 Host 校验用文档而非自动信任 X-Forwarded-*
  (自动信任会削弱 CSRF 防线,误配置即放行);mime 手工表而非引库(零依赖纪律,表以
  应用实际服务的类型为界,上传端点白名单与之对齐)。
- 验证:pc-server 250 项全绿(246+4);tsc 干净。

## 批次八修复记录(2026-07-27,卫生与单源化收尾)

### R3-6(P3)死代码三处 → 已修(实际五处,含孪生)

- ①tool-loop.ts fallbackToolCallId 整函数删除(全仓零引用),连带 id import 死引用清理;
  ②providers.ts readClaudeStreamingRound 的 currentEvent(声明/两处赋值/void 压 lint)四行删除;
  ③providers.ts fetchOpenAiAuxiliaryStream 的 appendUsageFromRaw(undefined, raw) 恒 no-op 删除,
  回调 raw 参数随之收窄。
- 勾连检查发现孪生:model-providers/checks.ts 两处同款 appendUsageFromRaw(undefined, raw)
  一并删除,import 清理。appendUsageFromRaw 其余三处调用均传真实 hooks.message,保留。

### R1-9(P3)注释锈蚀 → 已修(实际十二处,全仓扫尽)

- 原报两处:api/sse.ts:3 顶注(generating 实际早已从 conversations/generation-state 导入)、
  api/sse.ts:230 巨石行号(server.ts:10358/9601 → conversations/orchestrator.ts 模块指向)。
- rg 全仓扫描 server.ts:行号 与 server.ts:符号 两种锈蚀形态,又清十处孪生:pc-server 侧
  tools/definitions.ts(→search/index.ts)、scripts/request-chain-smoke.ts(→backup/export.ts)、
  conversations/orchestrator.ts(→api/sse.ts)、model-providers/checks.ts(→orchestrator)、
  backup/export.ts(→backup/import.ts);web-ui 侧 model-edit-dialog.tsx 四处、providers.tsx 一处
  (→model-providers/index.ts、inference-engine/providers.ts 等)。统一改模块名指向,不再写行号
  (行号必然再锈)。
- R3-5(readOpenAiStream 文案 10min vs 120s)经核已在批次三 R3-1 统一空闲看门狗时顺带修复
  (providers.ts 现注释记录在案),本批无需动作。

### R8-4(R6-5)types/settings.ts 手写镜像 → 已修(顶层单源 + 服务端类型补债)

- 可行性结论:整体 re-export 不可行也不应该——服务端对安卓契约透传域(mcpServers/searchServices/
  quickMessages/assistantTags/modeInjections/lorebooks/displaySetting/keybindings)故意存
  JsonValue/Record 不建模(备份往返不丢未知字段),前端在这些域需要 UI 视图形状;而 providers/
  assistants 服务端是严格存储形状,前端乐观构造需要宽松视图。两套形状都有存在理由。
- 落地方案(消除的是顶层键漂移面,即 R6-5 的病根):
  ①Settings 改为 extends Omit<ServerSettings, ViewRefinedKey>——顶层键单源自服务端,服务端新增
  字段(如 dismissedSearchServiceTypes)自动对前端类型可见;视图细化字段收敛进显式登记的
  ViewRefinedKey 清单(12 个),增删须两端同评审。前端镜像原本缺的 7 个字段(webServerJwtEnabled/
  searchCommonOptions/dismissedSearchServiceTypes/translateThinkingBudget/webDavConfig/s3Config/
  proxyConfig)全部补齐,Settings 的 [key: string]: unknown 索引签名(类型骗人的通道)删除。
  ②两端同形类型改 type-only re-export:记忆域六个(WriteStrategy/MemorySettings/MemoryEntry/
  AssistantMemoryGroup/PendingEntry/MemorySnapshot)+ ProxyConfig/ProxyMode/WebDavConfig/S3Config。
  ③手维护联合改派生:ModelType = Model["type"]、AsrProviderType/TtsProviderType 从服务端
  AsrProvider/TtsProvider 派生,AsrProviderProfile/TtsProviderProfile 改交集别名(新增 TTS 供应商
  不再需要前端同步改联合)。
- 勾连清理(漂移面的历史补丁全部拆除):proxy.tsx 本地手写 ProxyMode/ProxyConfig 镜像删除、
  data.tsx 本地 WebDavConfig/S3Config 镜像删除,两处改用单源类型;三个组件里 6 处 as Settings /
  as Record 强转删除;proxy/data 里与服务端 normalize 默认值重复的前端兜底对象删除(normalizeState
  保证字段永在且默认值同源)。data.tsx 恢复流两处 data.settings as Settings 保留(SSE 载荷本为
  unknown,属边界断言非漂移补丁)。
- 服务端类型补债(单源化的权威端必须先诚实):foundation/types Model 补上四个真实持久化字段
  (customHeaders/customBodies/providerOverwrite/manuallyAdded,均可选,normalize/enrichModel 展开
  透传),model-providers/index.ts 六处 (modelItem as any) 偷读与一处冗余 as JsonValue[] 全部消除。
  纯类型变更,零运行时行为差异。

### routes/settings.tsx Section 死成员 "plan" → 已修

- 联合类型删除 "plan"(navItems/渲染分支/?section= 深链校验全无此成员,纯死类型)。

### 验证

- pc-server 250 项全绿;web-ui 57 项全绿;双端 tsc 干净;web-ui 生产构建成功(@server type-only
  import 按 dto.ts 同机制擦除,产物不含服务端代码)。
- rg 死引用扫描:fallbackToolCallId / currentEvent / appendUsageFromRaw(undefined / server.ts:行号 /
  "plan" 全仓零残留。
