# Rikkahub Desktop 重构后全面复查计划(2026-07-26)

> 本轮性质:重构初步完成后的**新一轮零基审查**。要求以"第一次接触本项目"的视角审视,
> **刻意不参照** tmp_doc/项目重构方案.md(避免被既有方案思路框住);历史背景仅取自
> tmp_doc/项目全面重做.md 与用户口述(见下"背景速览")。
> 审查标准:成熟商业应用。架构合理性、可维护性、可拓展性、代码简洁度、bug、
> 用户场景覆盖、交互合理性、老用户迁移、Linux 二进制/Docker 部署、
> PC→APP / APP→PC / PC→PC 备份互操作。解决方案必须"终极",不容妥协。

## 工作协议(每次 compact 后先读本节)

1. **恢复流程**:compact 后先读本文件 + `全面复查发现-2026-07-26.md`,从下方"阶段清单"
   第一个未勾选阶段继续。不要重查已勾选阶段。
2. **只查不改**:本轮是诊断,发现的问题一律记录进发现文档,**不动代码**
   (修复在阶段九统一规划成批次,经用户确认后执行)。
3. **亲自读码**:不用子 agent。大文件用临时脚本分段读(bun -e 行号打印),复杂查询写
   `scripts/tmp-*.ts`(用完即删)。注意 `rg` 不要带 `-r`(那是替换标志)。
   **工具坑(阶段一实证)**:IDE 的 Read 工具在本仓库路径下会返回陈旧内容(bootstrap.ts
   读出了旧版本,漏掉实例锁调用),读文件一律用 `bun -e` 行号打印,勿信 Read 结果。
4. **发现记录格式**:写入发现文档对应阶段章节,编号 `R<阶段>-<序号>`,标注严重度:
   - P0 = 数据丢失/崩溃/安全洞,商业应用不可接受
   - P1 = 功能错误/明显卡顿/互操作破坏,用户可感知
   - P2 = 架构缺陷/可维护性差/边界未处理,迟早出事
   - P3 = 代码异味/冗余/不一致,顺手清理级
   每条含:证据(文件:行)、场景推演(corner case 怎么触发)、影响面、修复方向草案。
5. **每阶段收尾**:更新本文件勾选状态 + 在发现文档写该阶段小结(条数/严重度分布/
   最重要的 1-3 条),然后向用户汇报并等待 compact。

## 背景速览(代替重构方案文档的最小上下文)

- 起源:安卓 APP RikkaHub(第三方作者,源码在 /Rikkahub-Android)的桌面端重制,
  两端数据可经备份 zip 互迁(PC 单方面适配,APP 作者不配合,**互操作契约脆弱需重点审**)。
- 形态:Bun + TypeScript 服务端(pc-server,编译单 exe) + React Router v7 SPA(web-ui)
  + Tauri(Rust)壳,NSIS 安装包;另支持 Linux 二进制与 Docker(纯浏览器访问)。
- 历史顽疾(用户口述):HTTP/1.1 六连接饥饿(已两轮治理:events 单通道+SharedWorker);
  流式全量刷新(已改 SQLite 增量);架构臃肿冗余,维护困难 → 触发本次重做级重构。
- 重构已完成的大事(git log 可考):bootstrap 显式化、会话迁出 state.json 进 SQLite
  (DB-first + working-set 单实例注册表)、api/handlers 拆分、错误中心、事件通道合一、
  会话详情内存缓存、truncateIndex 对齐安卓、附件旁车抽取等。
- 未来路线(审架构时用此视角衡量可拓展性):多标签页 → 前端 UI/UX 重设计 →
  大文件流式上传+逐页解析 → 工作区/Agent 化。
- 版本:当前 1.5.0(测试构建阶段,用户正实测);老用户从 ≤1.4.1 升级的迁移路径必须无损。

## 全景盘点(2026-07-26,commit e153cea)

- 总量:ts/tsx/rs 共 296 文件、64,371 行(其中测试 3,466 行)。
- 服务端 pc-server ≈ 26k 行:inference-engine 4,099 / api 3,818 / conversations 2,909 /
  backup 2,709 / foundation 1,821 / tools 1,684 / search 1,271 / persistence 1,260 /
  media 1,091 / files 930 / model-providers 892 / memory 594 / scripts 2,958(工具与冒烟)。
- 前端 web-ui/app ≈ 37k 行:components 28,079(**前端复杂度大头**)/ routes 2,877 /
  lib 2,195 / services 670 / stores+hooks 872 / types 570 / workers 158。
- Tauri 壳 665 行 Rust。
- 最大文件:conversations.tsx 2,165 / settings/extensions.tsx 1,953 /
  inference-engine/providers.ts 1,654 / settings/providers.tsx 1,395 /
  conversation-sidebar.tsx 1,330 / settings/data.tsx 1,265 / chat-message.tsx 1,163。
- 测试占比 5.4%,集中在服务端(192 用例);前端几乎无测试(仅 api.sse.test.ts)。

## 阶段清单

- [x] **阶段一:服务端基座与生命周期**(~5k 行)已完成 2026-07-26:P1×2/P2×3/P3×6,见发现文档
  bootstrap.ts、server.ts、api/(router|request|sse|auth 相关)、persistence/json-store、
  foundation/(paths|types)、updates、单实例锁、优雅退出。
  Corner case 重点:双实例竞争;state.json 损坏/磁盘满/写一半断电;端口占用与随机端口;
  ≤1.4.1 数据目录升级(一次性迁移的幂等/中断恢复);流式进行中强杀进程;
  Docker/Linux 下路径与权限差异;时钟回拨对 Date.now() 序列的影响。

- [x] **阶段二:会话域(服务端)**(~3.5k 行)已完成 2026-07-26:P1×1/P2×2/P3×5(含 R1-5 收口降级),见发现文档
  conversations/(index|db|working-set|orchestrator|helpers|generation-state|stats|fts|
  read-queries|auxiliary)、api/handlers/conversations.ts、SQLite schema 与迁移、脏标记
  200ms 合并 flush。
  Corner case 重点:同会话并发生成/并发编辑;流式中删除/fork/移动/清库;working-set
  四条件清扫与 SSE 判活的竞态;分支树(selectIndex/兄弟消息)完整性;truncateIndex 越界;
  FTS 索引与正文失同步;5k+ 会话规模的分页/统计性能;WAL checkpoint 时机。

- [ ] **阶段三:推理引擎与工具链**(~5.8k 行)
  inference-engine/(providers|message-builder|events|各 loop)、tools/、search/、media/、
  model-providers/checks。
  Corner case 重点:各 provider 协议差异(Gemini/Claude/OpenAI 兼容层)的流解析健壮性;
  中途 abort 的资源回收与半消息落库;工具审批悬挂(重启后 pending 工具);MCP 外部进程
  生命周期;搜索/生图供应商失败降级;超长上下文截断与 token 统计口径;附件编码
  (base64 内联 vs 文件引用)在不同 provider 的分叉。

- [ ] **阶段四:文件域与备份互操作**(~4.6k 行)
  files/index、抽取旁车、OCR、backup/(export|import|storage|file-refs)、WebDAV/S3。
  Corner case 重点:安卓 zip 结构契约(settings.json/数据库表/附件命名)双向保真;
  导入中途失败的回滚;id 冲突与内容去重;>2GB 附件;zip 内路径穿越(zip-slip);
  导入时活动流式/打开的 SSE;PC→PC 跨版本(旧导新/新导旧);附件账本与磁盘失同步的自愈;
  抽取旁车迁移(1.4.1 → 1.5.0)幂等。

- [ ] **阶段五:服务端横切与部署面**(~3.5k 行)
  api/handlers/(settings|memory|system|errors)、app-config、assistants、memory、
  observability、Dockerfile、启动脚本、scripts/(bump-version|check-*)、更新检查。
  Corner case 重点:web 鉴权闸门覆盖面(所有端点?静态资源?SSE?);路径穿越
  (fonts/files/path 类端点);设置并发写丢失;记忆 pending 队列一致性;Docker 无持久卷/
  headless 场景;Linux 大小写敏感文件系统;反向代理后的 SSE(缓冲);更新检查的网络失败。

- [ ] **阶段六:前端基座与数据流**(~2.5k 行)
  root.tsx、services/(api|app-events)、workers/app-events-worker、stores/*、hooks/*、
  i18n、types(与服务端 DTO 的镜像一致性)。
  Corner case 重点:SharedWorker 各环境行为(Tauri WebView2/Firefox/Safari/降级路径);
  密码闸门与 401 风暴;SSE 重连风暴与退避;clockOffset 使用点;错误中心去重/风暴抑制;
  草稿 store 泄漏;i18n 键缺失回退。

- [ ] **阶段七:前端会话主界面**(~7k 行)
  routes/conversations.tsx、conversation-sidebar、message/(chat-message|message-part|
  parts/*|markdown)、input/(chat-input|model-list)、虚拟列表行为、草稿、快捷键。
  Corner case 重点:切换/流式/轮询三方竞态(本窗口刚改过详情缓存,重点回归);分支切换
  与编辑重发的树一致性;超长消息/巨型代码块渲染;IME 输入与快捷键冲突;拖拽上传边界;
  分享导出选择态;搜索命中定位;多窗口下同会话双开的行为。

- [ ] **阶段八:前端设置域与其余页面**(~9k 行)
  settings/(extensions|providers|data|speech|assistants|search|about|display|...)、
  model-edit-dialog、字体/主题、错误中心 UI、记忆 UI。
  Corner case 重点:provider CRUD 与模型列表一致性;扩展(MCP/Skill/世界书)配置的
  校验与坏配置容错;语音(TTS/ASR)设备权限;数据页导入导出的进度/失败反馈;
  i18n 双语完整性;表单未保存离开。

- [ ] **阶段九:综合诊断与修复批次规划**
  汇总各阶段发现,按严重度×影响面排序,划定修复批次(每批次可独立验证),
  产出面向用户的总结论:架构是否已达"成熟商业应用"标准、剩余风险清单、
  与未来路线(多标签/UI 重做/流式上传/Agent)的衔接评估。

## 附:各阶段通用检查清单

每个模块过一遍:
1. **职责边界**:单一职责?跨层伸手(如 handler 直摸 DB/全局可变状态)?循环依赖?
2. **错误路径**:每个 catch 是否有意义(上报/降级/注释意图)?错误是否到达用户?
3. **并发/竞态**:共享可变状态的写点;async 空窗期的状态假设;定时器/事件的清理。
4. **资源生命周期**:文件句柄/进程/连接/监听器的成对开关;泄漏点。
5. **数据契约**:DTO 双端一致?持久化格式版本化?向后兼容?
6. **规模退化**:数据量 ×100 时哪里先崩(全量遍历/全量序列化/无分页)。
7. **冗余与死代码**:重复实现、无引用导出、过时注释、临时脚本残留。
8. **测试覆盖**:关键路径有无回归锚点;测试是否测行为而非实现细节。
